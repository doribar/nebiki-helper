# 天候確認画面 表レイアウト変更報告

## バージョン

- appVersion: `2026.7.29-4`
- buildId: `build-20260729-173418-jst`
- dataSchemaVersion: `3`（変更なし）

## 変更内容

- 天候確認画面の時刻ごとのカードを廃止し、1つの表へ変更しました。
- 横方向に対象時刻、縦方向に天候・気温・風速を配置しました。
- 左端を52pxの行名列、残りを対象時刻数で均等分割する固定レイアウトにしました。
- 天候セルは既存の天候記号だけを表示し、天候名は可視表示していません。
- 気温セルと風速セルは数値だけを表示し、単位は行名側へ表示しました。
- 見出し、`入力を修正`、`この内容で確定`、二重実行防止処理は変更していません。

## 変更ファイル

- `src/components/screens/WeatherConfirmationPanel.tsx`
- `scripts/check-weather-confirmation.ts`
- `package.json`
- `package-lock.json`
- `CHANGE_REPORT_20260729_WEATHER_CONFIRMATION_TABLE.md`（本報告）

`src`配下は、確認画面コンポーネント以外に変更がないことをSHA-256比較で確認しました。

## 変更していないもの

- 天候入力と入力順
- 確認待ち状態と再読み込み復元
- `入力を修正`の動作
- `この内容で確定`の動作
- 二重実行防止
- 天候補正・値引率計算
- localStorage、日次JSON、19:00チェック、統合JSONの保存形式
- Supabase処理とSQL
- dataSchemaVersion

## 回帰テスト

- 全`check:*`: 17/17スクリプト成功、318/318成功
- 天候確認テスト: 13/13成功
- リファクタリングcharacterization: 成功
- TypeScript型チェック: 成功
- 本番ビルド: 成功
- PWA生成: 成功（generateSW、precache 10件、`sw.js`生成）

追加・更新した確認:

- 固定レイアウト表、thead/tbody、列・行見出しの存在
- 時刻・天候・気温・風速が同じ`hours`順で列を生成すること
- 気温・風速の単位が行名側にあり、各値セルは数値だけであること
- カード一覧と横スクロール領域が残っていないこと
- 15時の対象時刻が16〜21時の6列であること
- 既存の編集・確定・復元・二重実行防止テストが継続して成功すること

## 390×844px実画面確認

- viewport: `390 × 844px`
- document: `scrollWidth 390 / clientWidth 390`
- 表: `left 17px / right 373px / width 356px`
- 行名列: `52px`
- 16〜20時列: 各`50.66px`
- 21時列: `50.72px`
- ヘッダー・天候・気温・風速の各行で、全列のleft位置と幅が一致
- 天候行の可視文字: `天候☀☀☀☀☀☀`（天候名なし）
- 文字切れ、列ずれ、横スクロール、ボタン重なりなし
- `入力を修正`後に値を保持して戻り、再確認できることを確認
- `この内容で確定`を連続操作しても次画面へ一度だけ遷移
- console warning/error: 0件

## 未解決事項

- 本依頼に関する未解決事項はありません。
- Supabase SQLの変更・再実行は不要です。
