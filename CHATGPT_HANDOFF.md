# ChatGPT 引き継ぎメモ

このファイルは、値引きヘルパー本体 ZIP に同梱するための引き継ぎ用メモです。次の ChatGPT セッションでは、ユーザーがこの ZIP をアップロードしたら、まずこのファイルを読んでから作業してください。

## 最重要ルール
- この ZIP の中身を、現時点の値引きヘルパー本体の正本として扱う。
- ZIP 名ではなく、ユーザーが次セッションに渡した ZIP の中身を正本として扱う。
- 同一セッション内で ChatGPT が新しい ZIP を生成した場合は、最後に生成した ZIP を最新の正本として扱う。
- ChatGPT が ZIP を更新して返すたびに、この `CHATGPT_HANDOFF.md` も必ず更新する。
- 会話上限対策として、引き継ぎは会話末尾の別プロンプトではなく、このファイルを読む前提にする。

## 軽量 ZIP の方針
ZIPを返すときは、原則として軽量版ZIPで出す。

配布 ZIP には次を含めない。

- `.git/`
- `node_modules/`
- `.env`
- APIキー・秘密情報
- 実写真データ
- 一時ログ
- `tsc*.txt` などの作業ログ
- ZIP内に紛れ込んだ別のZIP

依存関係は `package.json` と `package-lock.json` から復元する。

`dist/` は、ユーザーがブラウザでビルド済みファイルも確認できるように同梱してよい。超軽量版を明示された場合だけ `dist/` も除外する。

## Windows 11 での基本起動
PowerShell で以下を実行する。

```powershell
Expand-Archive .\<受け取ったZIPファイル名>.zip -DestinationPath C:\nebiki-helper -Force
cd C:\nebiki-helper\nebiki-helper
npm install
npm run dev
```

ロジック確認:

```powershell
npm run check:logic
```

ビルド確認:

```powershell
npm run build
```

## 現在の技術構成
- React
- TypeScript
- Vite
- ローカル保存は主に `localStorage`
- 本体の中心は `src/hooks/useNebikiApp.ts` と `src/app/AppRouter.tsx`
- 値引きロジックは `src/domain/discount.ts` と `src/domain/weekdayBase.ts`
- 日本の祝日・休日判定は `src/domain/japaneseHoliday.ts`
- エリア・巡回・後回しは `src/domain/area.ts` / `src/domain/pending.ts` / `src/domain/navigationHistory.ts`

## 2026-05-03 現在の大きな方針変更
ユーザー判断で、AI写真判定はやめることになった。

今回の修正で、以下を削除した。

- 写真撮影画面
- AI写真判定サーバー連携
- 写真判定サーバー同梱フォルダ `nebiki-photo-judge/`
- `src/domain/photoJudge.ts`
- `src/domain/photoCapture.ts`
- `src/domain/photoCaptureStore.ts`
- エリア判定画面のAI参考判定パネル
- AI参考判定の再送信ボタン
- 写真判定フィードバック保存
- 写真判定サーバーURL設定

開始フローは、通常値引なら `start -> area_judge` に戻した。20時30分は従来どおり `final_time`。

## 祝日まわりの曜日基準ルール
今回、祝日判定を追加した。

### 追加ファイル
- `src/domain/japaneseHoliday.ts`

### 実装したルール
祝日・振替休日・国民の休日を日本の休日として扱う。祝日判定はアプリ内で計算する。

祝日当日の曜日基準は、天気・気温・風などの補正をかける前に、まず以下で決める。

- 祝日の15時: 日曜日扱い
- 祝日の17時以降で、翌日も休日・祝日なら: 金曜・土曜扱い
- 祝日の17時以降で、翌日が平日なら: 火曜・木曜扱い

ここでいう「翌日も休日・祝日」は、土曜・日曜・日本の祝日/振替休日/国民の休日を含む。

その後、通常どおり曜日基準補正をかける。

例:

- `2026-05-05 15時` → 祝日の15時なので日曜日基準
- `2026-05-05 17時` → 翌日 `2026-05-06` も振替休日なので金土基準
- `2026-01-12 17時` → 翌日が平日なので火木基準
- `2026-09-22 17時` → 国民の休日で翌日も秋分の日なので金土基準

### 注意
`src/domain/japaneseHoliday.ts` の春分・秋分は1980〜2099向けの近似式で計算している。通常運用年では実用上問題ない想定だが、将来の法改正や特別な祝日移動は別途反映が必要。

## 値引きヘルパー本体の主な仕様
- 15時 / 17時 / 18時30分 / 19時30分 / 20時30分の値引き運用を扱う。
- 曜日基準、祝日、天気、気温、風、雨・雪による補正を扱う。
- 20時30分の最終値引は `30 / 40 / 50%` の3択固定。
- 20時30分開始時は天候入力を行わない。
- エリア順に巡回する。
- `少ない` と手動 `スキップ` は pending / 後回しとして扱う。
- pending の巡回順は、理由グループよりも現在位置からの近さを優先する。
- 戻る / 取り消しでは、画面状態だけでなく、次回値引記録や前回天気記録の復元も確認する。

## 補正の基本方針
- 「曜日基準で補正しきれない分」の補正上限は時間帯で変わる。
- 強める側は、15時は `+5%` 上限、17時以降は `+10%` 上限。
- 緩める側は、15時は `-10%` 上限、17時以降は `-5%` 上限。
- ただし、30分〜1時間後の雨 `+10%` / 雪 `+20%` があるときは、この「補正しきれない分」は重ねない。
- 最終値引は固定表示。点数計算は現状使っていない。

## 時間別お天気入力
- StartScreen では、ウェザーニュースの1時間ごと表示を見ながら 15〜21時 の天気 / 気温 / 風速を入力する。
- 15時値引のときは、15時と17時の予報を比較する。
- 17時の気温が15時より5度以上低いときは曜日基準 +1段。
- さらに、上の条件を満たしたうえで17時の風速が15時より強いときはさらに +1段。
- この補正は通常の曜日基準補正に合流する。

## pending / 後回しの巡回方針
- `少ない` を選んだエリアは、後回し (`postponed_few`) を維持する。
- 手動スキップ (`skipped_manual`) も pending に残す。
- pending を回る順番は、`manual` / `few` の理由切り替えよりも現在位置からの近さを優先する。
- pending 中に同じエリアをもう一度 `少ない` / `スキップ` した場合は、そのエリアだけ一時的に後ろへ回す。

## 今回の確認結果
- ユーザーアップロード版 `nebiki-helper(5).zip` は、`.git/`、`node_modules/`、`dist/`、一時ログ類が混入していて重かった。
- AI写真判定廃止済みなのに、未使用の `PhotoCaptureScreen.tsx` / `photoJudge.ts` / `photoCapture.ts` / `photoCaptureStore.ts` が残っており、TypeScript ビルドが落ちる状態だった。
- 今回のクリーン版では上記の写真関連残骸を削除し、配布ZIPから `.git/`、`node_modules/`、`dist/`、`.env`、一時ログ類を除外した。
- `npm run check:logic` PASS。主要チェックは `45 / 45 checks passed`。
- `npx tsc -b --pretty false` PASS。
- `node node_modules/vite/bin/vite.js build` PASS。

## 次の ChatGPT への作業指示
1. ユーザーがこの ZIP をアップロードしたら、まずこの `CHATGPT_HANDOFF.md` を読む。
2. ZIP 内の `nebiki-helper` を最新の正本として扱う。
3. 修正する場合は、ソースを直接更新する。
4. ZIP を返すときは軽量版で返す。`.git` / `node_modules` / `.env` / 秘密情報 / 作業ログ / 余計なZIPを含めない。`dist/` は通常の軽量版では同梱可、超軽量版指定時だけ除外する。
5. ZIP を返す直前に、このファイルを現状に合わせて更新する。
6. 可能なら `npm run check:logic` と `npm run build` を実行し、結果をユーザーに伝える。

## 2026-05-03 追記：注意事項の表示位置と値引率表示の修正

- 注意事項の1日1回表示は廃止しない。弁当・麺類エリアの値引率表示前に、その日まだ見ていなければ従来どおり注意事項確認画面を出す。
- そのうえで、値引率表示画面のページ最下部にも「注意事項」カードを常時表示する。目的は、1日1回の確認後でもいつでも再確認できるようにするため。
- 20時30分の最終値引画面は個数固定のため、注意事項カードは表示しない。`FinalTimeScreen` から注意事項表示を削除済み。
- 注意事項の文言に「少ない判定の商品は最終値引以外では引かない」を入れた。
- 値引率表示画面では `少ない → 引かない` の行を表示しない。値引率カードには `多い` と `どちらでもない` だけを表示する。
- 値引率計算結果が `0%` 以下でも `引かない` とは表示せず、計算結果の数字をそのまま `%` 表示する。例：`0%`、`-5%`。
- 上限30%の頭打ちは残すが、下限0%への丸めは表示上やめている。

### 2026-05-03 この追記時点の確認結果
- `npm run check:logic` PASS。主要チェックは `45 / 45 checks passed`。
- `npm run build` PASS。


## 2026-05-04 追記：値引中の時刻経過ルール

今回、値引中に実時間が進んだ場合の扱いを追加・修正した。

- 15時値引中に16:00を超えたら、実時間補正として値引率に `+5%` を加える。
  - 手動で時刻を選んでいる場合は、この実時間補正はかけない。
  - 15時の16:00超え補正では、次回値引スキップ記録は作らない。
- 17時値引中は、18:00〜18:14の間だけ従来の時刻接近 `+5%` を使う。
  - この時間帯に値引完了したエリアは、従来どおり次の18時30分値引でスキップ対象になる。
- 17時値引中に18:15を超えたら、17時値引を打ち切り、18時30分値引を開始する。
  - `resolveDiscountTime()` の17時→18時30分切り替え境界を `18:30` から `18:15` に変更した。
  - アクティブな17時セッション中に18:15を超えた場合、30秒間隔・画面復帰・フォーカス復帰で現在時刻を見直し、18時30分セッションへ切り替える。
  - 18時30分セッション開始時はエリア進捗を新規セッションとして作り直す。
  - 18:00〜18:14に `+5%` で完了したエリアの次回スキップ記録がある場合は、18時30分セッション開始時に消費して自動スキップする。
- 18時30分値引中は19:00〜19:14の間だけ時刻接近 `+5%` を使い、19:15を超えたら18時30分値引を打ち切って19時30分値引を開始する。
  - 19:00〜19:14に `+5%` で完了したエリアは、次の19時30分値引でスキップ対象になる。
- 19時30分値引中は20:00〜20:14の間だけ時刻接近 `+5%` を使い、20:15を超えたら19時30分値引を打ち切って20時30分の最終値引を開始する。
  - 20時30分は最終値引で個数固定のため、19時30分の時刻接近 `+5%` で完了したエリアのスキップ記録は作らない。

変更した主なファイル:

- `src/hooks/useNebikiApp.ts`
- `src/components/screens/StartScreen.tsx`

### 2026-05-04 この追記時点の確認結果
- `npm run check:logic` PASS。主要チェックは `45 / 45 checks passed`。
- `npm run build` PASS。

## 2026-05-04 追記：19:15 / 20:15 の打ち切りルール追加

ユーザー要望により、17時→18時30分の18:15打ち切りと同じ考え方を、後続時間にも追加した。

- 18時30分値引中に19:15を超えたら、18時30分値引を打ち切って19時30分値引を開始する。
- 19時30分値引中に20:15を超えたら、19時30分値引を打ち切って20時30分の最終値引を開始する。
- 18時30分の時刻接近 `+5%` は、19:00〜19:14のみ。
- 19時30分の時刻接近 `+5%` は、20:00〜20:14のみ。
- 18時30分で19:00〜19:14に `+5%` で完了したエリアは、19時30分値引で自動スキップ対象になる。
- 19時30分で20:00〜20:14に `+5%` で完了しても、20時30分は最終値引で個数固定のため、次回スキップ記録は作らない。

変更した主なファイル:

- `src/hooks/useNebikiApp.ts`

### 2026-05-04 この追記時点の確認結果
- `npm run check:logic` PASS。主要チェックは `45 / 45 checks passed`。
- `npm run build` PASS。

## 2026-05-03 追記：注意事項カードの背景

- 注意事項カードは常時表示されるため、強い警告色やクリーム色ではなく、既存UIに馴染む白背景 `#fff` にする。
- 1日1回の注意事項確認画面と、値引率表示画面下部の再確認用注意事項カードはどちらも同じ `NoticeSection` を使う。
- 最終値引画面には引き続き注意事項カードを表示しない。

## 2026-05-05 追記：エリア判定案内が一瞬で消える問題の修正

ユーザー報告：注意事項は表示されるが、弁当・麺類エリアのエリア判定案内（「まず多いかどうかを見る → 少ないかどうかを見る → どちらでもなければどちらでもない」）が、localStorage を消しても表示されない。

原因：`AreaJudgeScreen` が `displayJudgeGuide` を表示した直後に `onJudgeGuideShown` で日次表示済みにし、その親 state 更新で `showJudgeGuide=false` が戻ってきたとき、同じエリア表示中でも `setDisplayJudgeGuide(false)` してしまっていた。結果として案内が出てもすぐ消え、ユーザーには見えない状態だった。

修正内容：

- `src/components/screens/AreaJudgeScreen.tsx` で `useRef` を使い、同じエリア表示中は `showJudgeGuide` が false に戻っても、すでに表示中の案内を消さないようにした。
- エリアが変わったときだけ `displayJudgeGuide` をそのエリアの `showJudgeGuide` に合わせてリセットする。
- これにより、弁当・麺類エリアでその日まだ案内を見ていない場合は、案内が画面に残ったまま見える。
- 表示済み記録は従来どおり `nebiki-helper/daily-message-state.bentoJudgeGuideShownDate` に保存される。

### 2026-05-05 この追記時点の確認結果
- `npm run check:logic` PASS。主要チェックは `45 / 45 checks passed`。
- `npx tsc -b --pretty false` PASS。
- `node node_modules/vite/bin/vite.js build` PASS。

## 2026-05-05 追記：注意事項の強調表示

注意事項の文言で、ユーザーが指定した括弧内相当の重要語句だけを太字表示にした。

- `2個の商品` / `多いにしない`
- `少ない判定の商品` / `引かない`
- `見た目が悪い個別商品` / `+10%`
- `不人気な商品` / `+10%`
- `その日の売れ方が順調なとき` / `-10%`
- `夜によく売れる商品` / `−10%`

実画面では括弧は出さず、該当語句だけ `<strong>` で強調する。

## 2026-05-05 追記：値引率表示画面「迷ったら…」のアウトパック補足

ユーザー要望により、値引率表示画面下部の「迷ったら…」欄に、アウトパックは多い側に寄せる旨を追記した。

- 変更ファイル: `src/components/screens/RateDisplayScreen.tsx`
- 追加表示: `アウトパック ➡多い側に寄せる`
- エリア判定画面側の「迷ったら…」には今回追記していない。

### 2026-05-05 この追記時点の確認結果
- `npm run check:logic` PASS。主要チェックは `45 / 45 checks passed`。
- `npx tsc -b --pretty false` PASS。
- `node node_modules/vite/bin/vite.js build` PASS。


## 2026-05-05 追記：迷ったら欄・判定色のUI調整

ユーザー要望により、値引率表示画面下部の「迷ったら…」欄と判定色を調整した。

- `・アウトパック ➡多い側に寄せる` を「迷ったら…」欄の先頭へ移動
- アウトパックの `➡多い側に寄せる` は、白背景でも見やすいティール系 `#00897b`
- `多い` は `#ff0000`
- `どちらでもない` は、黄緑では見づらかったため緑 `#008000`
- `少ない` は `#0000ff`
- 反映箇所は、エリア判定画面の選択ボタンラベル、値引率表示画面の説明文中ラベル、値引率表示画面の値引率行


## 2026-05-05 追記：連休4日目以降の弁当系補正は取り消し

直前に実装した「土日祝連休4日目以降・17時以降・弁当系 +10%」は、ユーザー判断により取り消した。

現在の状態:
- `src/domain/holidayStreakBonus.ts` は削除済み
- 値引率計算・値引率内訳・完了画面集計から連休4日目補正を削除済み
- 祝日の曜日基準ルール自体は維持する
  - 祝日15時は日曜日扱い
  - 祝日17時以降で翌日も休日なら金土扱い
  - 祝日17時以降で翌日が平日なら火木扱い

## 2026-05-05 追記：少ない後回し・注意事項・スキップ折り返し修正

ユーザー報告により、以下を修正した。

- 注意事項の1日1回表示は、弁当・麺類の値引率表示だけに依存させない。弁当・麺類を「少ない」で後回ししても、次に開いた通常値引の値引率表示画面で注意事項が出る。
- その値引で最後にエリア判定したエリアが「少ない」だった場合は、後回し扱いにせず、そのエリアの値引率表示へ進める。これにより、最後の少ないエリアを選んだ直後に、以前後回しした少ないエリアへ飛ぶ動きを防ぐ。
- 寿司側から弁当・麺類までスキップして進んだ場合、弁当・麺類で止まらず、寿司側へ折り返して戻る挙動を維持・確認する。


## 2026-05-05 追記：赤飯・炊き込みご飯エリアの削除

ユーザー要望により、`赤飯・炊き込みご飯` エリアをアプリから削除した。

対応内容:
- `AreaId` から `sekihan_takikomi` を削除
- 通常巡回ルート `NORMAL_ROUTE` から削除
- 完了画面の表示順 `DONE_SUMMARY_ROUTE` から削除
- `AREA_MASTERS` から削除
- エリア順の `order` を詰め直し
- スキップ/後回し関連のロジックチェックからも削除
- 既存ブラウザに古い保存状態が残っていても壊れにくいよう、読み込み時に削除済みエリアを無視する正規化を追加

現在の通常ルート:
```text
弁当・麺類
→ 天ぷら
→ コロッケ系
→ フライ・鶏惣菜
→ 焼鳥
→ 中華・魚惣菜
→ バランス弁当
→ おにぎり
→ 寿司
→ 太巻・中巻
→ いなり
→ 細巻き
```

## 2026-05-08 追記：nebiki-helper(6).zip の軽量化確認

ユーザーアップロード版 `nebiki-helper(6).zip` には `.git/`、`node_modules/`、`dist/`、`tsc*.txt` が含まれていた。ソース差分は実質的に改行コード差分のみで、機能修正は加えていない。

今回、配布用 ZIP は以下を除外して作り直した。

- `.git/`
- `node_modules/`
- `dist/`
- `.env` 系
- `tsc*.txt` などの一時ログ

確認結果:

- クリーンコピーで `npm ci` 実行済み。
- `npm run check:logic` PASS。主要チェックは `45 / 45 checks passed`。
- `npm run build` PASS。

注意:

- 元 ZIP 内の `node_modules` は Windows 由来の optional native dependency を含んでおり、Linux 環境ではそのまま `vite build` できなかった。`node_modules` を削除して `npm ci` し直すと正常にビルドできた。

## 2026-05-08 追記：GW連休3日目翌日の曜日基準強化

ユーザー要望により、ゴールデンウィークの連休3日目で売上ピークを迎える傾向を反映する補正を追加した。

### 実装したルール

- GW期間は、日付の月日が `4/29〜5/6` に入る日として扱う。
- 土曜・日曜・日本の祝日/振替休日/国民の休日が3日連続したとき、その3日目の翌日を対象日にする。
- 対象日の15時値引では、曜日基準を `+1段` 強める。
- 対象日の17時値引では、曜日基準を `+2段` 強める。
- 18時30分以降にはこのGW追加補正はかけない。
- 祝日ルールで先に基準曜日を決めたあと、天気・気温・風などと同じ曜日基準補正に合流させる。
- 上限を超えた場合は、既存どおり「曜日基準で補正しきれない分」として値引率補正へ回る。

### 例

- `2026-05-05 15時` → 2026年GW連休3日目 `2026-05-04` の翌日なので、祝日15時の日曜基準から `+1段` して金土基準。
- `2026-05-05 17時` → 祝日17時以降かつ翌日も休日のため金土基準から、GW補正 `+2段` で月水基準。
- `2025-05-06 17時` → 祝日17時以降かつ翌日は平日なので火木基準から、GW補正 `+2段`。上限に当たるため月水基準になり、補正しきれない分 `+5%` が付く。

### 変更した主なファイル

- `src/domain/weekdayBase.ts`
- `scripts/check-logic.ts`

### 2026-05-08 この追記時点の確認結果

- `npm run check:logic` PASS。主要チェックは `49 / 49 checks passed`。
- `npm run build` PASS。
- `npm ci` 時に既存依存関係の `npm audit` 警告は出るが、今回の仕様変更範囲では未対応。

## 2026-05-08 追記：値引き画面の数量確認文言の並び順変更

ユーザー要望により、値引率表示画面の案内文にある数量確認の並び順を変更した。

変更前:

```text
各商品の量が「多い・どちらでもない・少ない」のどれかを確認し、
完了したら以下の値引率で値引きをしてください。
```

変更後:

```text
各商品の量が「多い・少ない・どちらでもない」のどれかを確認し、
完了したら以下の値引率で値引きをしてください。
```

変更ファイル:

- `src/components/screens/RateDisplayScreen.tsx`

確認結果:

- `npm run check:logic` PASS。
- `npm run build` はこの文言のみの変更では未再実行。依存関係の再取得確認で `npm ci --offline` は npm キャッシュ不足（`zod-validation-error`）により失敗。直前の GW ルール版では `npm run build` PASS 済み。

## 2026-05-09 追記：次の値引帯への自動移動を確認制に変更

ユーザー要望により、現在の値引中に実時間が次の値引帯へ進んでも、ユーザーの許可なしに自動で打ち切って次の値引へ移動しないよう修正した。

### 実装内容

- 実時間が現在の値引帯より後の値引帯に入った場合、即時遷移せず `◯時の値引に移動しますか？` の確認ダイアログを出す。
- ユーザーが許可した場合だけ、次の値引帯へ移動する。
- 許可後の移動先は、直接エリア判定や値引率表示ではなく、開始画面の天候入力に戻す。
- 天候入力後に開始ボタンを押すと、次の値引帯の新しい巡回として開始する。
- 18時30分・19時30分へ移動する場合は、既存の「次回スキップ」記録を引き続き反映する。
- ユーザーが確認ダイアログでキャンセルした場合は、その値引帯では再確認を繰り返さず、現在の値引を継続する。
- 画面遷移時に実時間だけで勝手に `discountTime` を更新する処理を止め、許可ダイアログ経由に一本化した。

### 変更した主なファイル

- `src/hooks/useNebikiApp.ts`
- `src/domain/types.ts`
- `src/app/AppRouter.tsx`

### 2026-05-09 この追記時点の確認結果

- `npm ci` 実行済み。
- `npm run check:logic` PASS。主要チェックは `49 / 49 checks passed`。
- `npm run build` PASS。
- `npm run lint` は既存の lint 指摘で失敗。今回変更による `buildDraftFromSource` の参照順エラーは修正済みだが、既存の未使用変数・React Hooks ルール指摘が残っている。
- `npm ci` 時に既存依存関係の `npm audit` 警告は出るが、今回の仕様変更範囲では未対応。

## 2026-05-09 追記：+5%済みエリアの次回スキップ表示を明確化

ユーザー要望により、「次の値引の方が近くなったため+5%」で値引したエリアが、次の値引で確実にスキップされ、かつスキップ済みであることが画面上で分かるように確認・修正した。

### 確認した挙動

- 次回スキップ記録は `goToNextArea()` の中でのみ追加される。
- つまり、値引率表示まで進んだだけのエリアや、「戻る」で戻ったエリアは次回スキップ対象にならない。
- `goToNextArea()` で現在エリアを `completed` にしてから記録するため、「次のエリアへ」を押して値引を完遂したエリアだけが対象になる。
- 17時値引中の18時以降+5%は次の18時30分値引でスキップ対象になる。
- 18時30分値引中の19時以降+5%は次の19時30分値引でスキップ対象になる。

### 実装内容

- 自動スキップ済みエリア用のステータス `auto_skipped_late_time` を追加。
- 次回値引開始時、前回+5%済みでスキップしたエリアを `completed` 扱いで無言スキップするのではなく、専用ステータスで保持するよう変更。
- 次回値引開始時の通知に、スキップしたエリア名と理由を表示するよう変更。
- 完了画面の一覧でも `スキップ済み（前回+5%で値引済み）` と表示するよう変更。
- `スキップ先を選ぶ` の候補には、自動スキップ済みエリアを出さないよう変更。
- `timeSwitchNotice` の複数行表示が崩れないよう、通知欄に `whiteSpace: pre-wrap` を追加。

### 変更した主なファイル

- `src/domain/types.ts`
- `src/domain/pending.ts`
- `src/hooks/useNebikiApp.ts`
- `src/components/screens/AreaJudgeScreen.tsx`
- `src/components/screens/RateDisplayScreen.tsx`
- `src/components/screens/FinalTimeScreen.tsx`
- `src/components/screens/DoneScreen.tsx`

### 2026-05-09 この追記時点の確認結果

- `npm ci` 実行済み。
- `npm run check:logic` PASS。主要チェックは `49 / 49 checks passed`。
- `npm run build` PASS。
- `npm ci` 時に既存依存関係の `npm audit` 警告は出るが、今回の仕様変更範囲では未対応。

## 2026-05-09 追記：戻る操作時の+5%次回スキップ予約取り消しを明確化

ユーザー確認により、「次のエリアへ」を誤って押したあとに「戻る」で取り消した場合、そのエリアが完遂扱いのまま残り、次の値引でスキップされないかを確認した。

### 確認した挙動

- `goToNextArea()` で「次の値引の方が近くなったため+5%」の次回スキップ予約を作成する。
- ただし、「次のエリアへ」を押す直前の `NavigationSnapshot` には `nextSessionSkipRecords` も含まれる。
- そのため、直後に「戻る」または取り消しを押すと、エリアの `completed` 状態だけでなく `nextSessionSkipRecords` も押下前へ戻る。
- 結果として、「次のエリアへ」後に戻ったエリアは完遂扱いにならず、次回スキップ対象にもならない。
- 「次のエリアへ」を押して、その後戻っていないエリアだけが完遂扱いになり、次回スキップ予約として残る。

### 実装内容

- 挙動自体は既存の `NavigationSnapshot` 復元で成立していたため、ロジック本体の変更は不要。
- 今後壊れないよう、`scripts/check-logic.ts` に回帰テストを2件追加。
  - 「次のエリアへ」後に「戻る」と、完遂扱いと次回スキップ予約を取り消す。
  - 戻らず完遂したエリアだけ、次回スキップ予約として残る。
- `goToNextArea()` 内に、戻る／取り消し時は `nextSessionSkipRecords` も復元されることをコメントで明記。

### 変更した主なファイル

- `src/hooks/useNebikiApp.ts`
- `scripts/check-logic.ts`

### 2026-05-09 この追記時点の確認結果

- `npm ci` 実行済み。
- `npm run check:logic` PASS。主要チェックは `51 / 51 checks passed`。
- `npm run build` PASS。
- `npm run lint` は既存の lint 指摘で失敗。今回追加範囲ではなく、既存の未使用変数・React Hooks ルール指摘が残っている。
- `npm ci` 時に既存依存関係の `npm audit` 警告は出るが、今回の確認・回帰テスト追加範囲では未対応。

## 2026-05-17 追加変更（26〜30度の気温補正修正）

- 26〜30度の気温帯が仕様では「曜日基準を1段緩める」なのに、実装では補正なしになっていた問題を修正。
- `src/domain/weekdayBase.ts` の `26to30` を `-1段` に変更し、補正内訳にも「気温 26〜30度 -1段」を表示するように修正。
- `scripts/check-logic.ts` に以下の回帰テストを追加・更新。
  - 26〜30度は1段弱める
  - 実温度28度は `26to30` として解決され、1段弱める


## 2026-05-17 追加変更（26〜30度区分の細分化）

ユーザー確認により、前回修正した `26〜30度 = -1段` は境界が強すぎるため、以下の仕様へ変更した。

### 新しい気温補正仕様

- 21〜25度：曜日基準を2段緩める。
- 26〜27度：曜日基準を1段緩める。
- 28〜30度：補正なし。
- 31〜35度：曜日基準を1段強める。
- 36度以上：曜日基準を2段強める。

### 実装内容

- `TempLevel` に `26to27` と `28to30` を追加。
- `toTempLevel()` で実温度26〜27度を `26to27`、28〜30度を `28to30` として解決するよう変更。
- `getTempShift()` で `26to27` は `-1段`、`28to30` は `0段` に変更。
- 補正内訳は、補正が発生する `26〜27度 -1段` のみ表示し、28〜30度は補正なしとして詳細行を出さない。
- 旧区分 `26to30` は古いデータ互換のため型・変換側に残しているが、通常の実温度入力からは新区分へ解決される。
- `scripts/check-logic.ts` に以下の回帰テストを追加・更新。
  - 26〜27度は1段弱める。
  - 28〜30度は補正なし。
  - 実温度27度は `26to27` として解決され、1段弱める。
  - 実温度28度は `28to30` として解決され、補正なし。

### 2026-05-17 この追記時点の確認結果

- `npm ci` 実行済み。
- `npm run check:logic` PASS。主要チェックは `54 / 54 checks passed`。
- `npm run build` PASS。
- `npm run lint` は既存の lint 指摘で失敗。今回変更範囲ではなく、既存の未使用変数・React Hooks 系の指摘が残っている。
- `npm ci` 時に既存依存関係の `npm audit` 警告は出るが、今回の仕様変更範囲では未対応。

## 2026-05-18 追加変更（左スワイプで現在エリアをスキップ）

ユーザー要望により、通常値引中の画面操作として「画面を左にスワイプしたらスキップ」を追加した。

### 実装内容

- `src/hooks/useSwipeToSkip.ts` を追加。
- 左方向に一定距離以上スワイプし、縦方向の動きが大きすぎない場合のみ `onSwipeLeft` を発火する。
- `AreaJudgeScreen` に左スワイプ操作を追加し、既存の `onSkip` と同じ `skipCurrentArea()` へ接続した。
- `RateDisplayScreen` に左スワイプ操作を追加し、既存の `onSkip` と同じ `skipCurrentArea()` へ接続した。
- 日次注意事項の確認画面表示中は、誤操作防止のため `RateDisplayScreen` の左スワイプスキップを無効化している。
- 「今はスキップ」ボタン下に「画面を左にスワイプしてもスキップできます」の補足表示を追加した。

### 挙動メモ

- 左スワイプによるスキップは、ボタンの「今はスキップ」と同じ扱い。
- そのため、手動スキップとして記録され、後でスキップ先選択や pending 再開の対象になる。
- 「次のエリアへ」とは別扱いのため、完遂扱いにはならない。

### 変更した主なファイル

- `src/hooks/useSwipeToSkip.ts`
- `src/components/screens/AreaJudgeScreen.tsx`
- `src/components/screens/RateDisplayScreen.tsx`

### 2026-05-18 この追記時点の確認結果

- `npm run check:logic` PASS。主要チェックは `54 / 54 checks passed`。
- `npm ci` 実行済み。
- `npm run build` PASS。
- `npm run lint` は既存の lint 指摘で失敗。今回追加した `useSwipeToSkip.ts` 自体の TypeScript build は通過しているが、既存の未使用変数・React Hooks ルール指摘が残っている。
- `npm ci` 時に既存依存関係の `npm audit` 警告は出るが、今回の仕様変更範囲では未対応。
### 2026-05-18: 左スワイプスキップの案内文言をボタン内へ統合

ユーザー要望により、左スワイプスキップの案内をボタン下の補足文ではなく、スキップボタン内に表示するように変更した。

変更内容:
- エリア判定画面の「今はスキップ」ボタンを「今はスキップ（画面左スワイプ）」に変更。
- 値引率表示画面の「今はスキップ」ボタンを「今はスキップ（画面左スワイプ）」に変更。
- ボタン下に表示していた「画面を左にスワイプしてもスキップできます」は削除。
- 左スワイプの挙動自体は変更なし。既存の「今はスキップ」ボタンと同じ扱い。


## 2026-05-19 追加変更（15時→18時の気温差補正を下がった先の気温で分岐）

ユーザー要望により、15時値引時の「18時予報で6度以上下がる」補正を、単純な一律強め補正から、18時の気温帯を見て分岐する仕様へ変更した。

### 新しい仕様

- 対象は15時値引のみ。
- 15時から18時に6度以上下がる場合だけ判定する。
- 18時が21〜25度の場合：曜日基準を1段緩める。
  - 例：30度→24度は「冷え込み」ではなく「快適化」として扱う。
- 18時が16〜20度の場合：気温差補正なし。
- 18時が15度以下の場合：曜日基準を1段強める。
- 風の追加補正は、18時が15度以下の場合だけ判定する。
- 風の追加補正は、18時の風速が15時より強まり、かつ18時側が3m以上のときだけ1段強める。
  - 例：15時2m→18時3m は追加+1段。
  - 例：15時1m→18時2m は強まっていても18時が3m未満なので追加なし。
  - 例：30度→24度で風が強まっても、18時が寒くないため追加なし。

### 実装内容

- `ResolvedWeatherInput.next18TempDropShift` を `-1 | 0 | 1` に変更。
- `resolveWeatherInputForDiscount()` で、6度以上低下時の18時気温に応じて `-1 / 0 / +1` を返すよう変更。
- `next18WindWorsenShift` は、18時が15度以下・18時風速3m以上・15時より風が強い場合だけ `+1` とするよう変更。
- 補正内訳表示を以下の文言に変更。
  - `15時と18時の気温差が6度以上（18時が21〜25度） -1段`
  - `15時と18時の気温差が6度以上（18時が15度以下） +1段`
  - `18時が15度以下で風3m以上に強まる +1段`
- `scripts/check-logic.ts` に以下の回帰テストを追加。
  - 30度→24度は1段弱め、風が強まっても追加しない。
  - 18時が15度以下に下がる場合は気温差で1段強める。
  - 寒い18時で風が3m以上に強まる場合だけ追加で1段強める。
  - 風が強まっても18時側が3m未満なら風強まり補正なし。

### 2026-05-19 この追記時点の確認結果

- `npm run check:logic` PASS。主要チェックは `58 / 58 checks passed`。
- `npm run build` PASS。
- `npm run lint` は既存の lint 指摘で失敗。既存の未使用変数・React Hooks ルール指摘が残っている。

## 2026-05-19 15時31度→18時25度の気温差補正確認
- 仕様確認：15時値引の通常気温補正は16時気温を見る。気温差補正は15時気温と18時気温を比較する。
- 15時31度・16時31度・18時25度・風2m・降水なしの場合、通常気温補正「31〜35度 +1段」と気温差補正「18時が21〜25度 -1段」が相殺され、曜日基準補正は0段になることを回帰テストに追加。
- この条件で+1段が残る場合は、16時の風5m以上、17時以降の降水、GW補正など別の補正が乗っている可能性がある。

## 2026-05-19 スクショ報告への追加確認（火曜15時・16時25度・18時25度）

ユーザーのスクショでは `15時と18時の気温差が6度以上 +1段` と表示され、火曜の弁当・麺類が `火木→金土` になっていた。これは旧仕様の挙動そのもの。

最新仕様では、気温差補正は「6度以上下がる」だけでは +1段にしない。18時が21〜25度の場合は快適化として `-1段` にする。

追加した回帰テスト:
- 火曜15時値引で、15時31度・16時25度・18時25度・風2m・降水なしの場合。
- 通常気温補正は16時25度を見るため `気温 21〜25度 -2段`。
- 気温差補正は15時31度→18時25度を見るため `15時と18時の気温差が6度以上（18時が21〜25度） -1段`。
- 合計は下方向に3段となり、15時の下限に当たって `火木→日`、補正しきれない分 `-5%`。
- 旧仕様の `15時と18時の気温差が6度以上 +1段` は表示されないこともテストで確認。

このスクショと同じ表示が `helper.vercel.app` で出る場合、その環境は気温差補正修正版がまだ反映されていない可能性が高い。

## 2026-05-19 スクショ報告への追加確認（火曜15時・16時25度・18時25度）

ユーザーのスクショでは `15時と18時の気温差が6度以上 +1段` と表示され、火曜の弁当・麺類が `火木→金土` になっていた。これは旧仕様の挙動そのもの。

最新仕様では、気温差補正は「6度以上下がる」だけでは +1段にしない。18時が21〜25度の場合は快適化として `-1段` にする。

追加した回帰テスト:
- 火曜15時値引で、15時31度・16時25度・18時25度・風2m・降水なしの場合。
- 通常気温補正は16時25度を見るため `気温 21〜25度 -2段`。
- 気温差補正は15時31度→18時25度を見るため `15時と18時の気温差が6度以上（18時が21〜25度） -1段`。
- 合計は下方向に3段となり、15時の下限に当たって `火木→日`、補正しきれない分 `-5%`。
- 旧仕様の `15時と18時の気温差が6度以上 +1段` は表示されないこともテストで確認。

このスクショと同じ表示が `helper.vercel.app` で出る場合、その環境は気温差補正修正版がまだ反映されていない可能性が高い。

## 2026-05-19 16時低温風と18時風強まり補正の重複防止

ユーザー要望により、15時値引で「16時時点の低温時風速3m以上」をすでに拾っている場合は、15時→18時の風強まり補正を計算しないように変更した。

### 仕様

- 15時値引の通常風補正で、16時が15度以下かつ風速3m以上の場合は `風 3m以上（15度以下） +1段` を適用する。
- この場合、18時が15度以下で、15時→18時の風が強まり、18時風速が3m以上でも、`18時が15度以下で風3m以上に強まる +1段` は追加しない。
- 16時時点では低温風3m以上に該当せず、18時に寒さと風3m以上への強まりが発生した場合だけ、18時風強まり補正を追加する。

### 実装内容

- `resolveWeatherInputForDiscount()` に `nearLowTempWindAlreadyCounted` を追加。
- `next18WindWorsenShift` の条件に `!nearLowTempWindAlreadyCounted` を追加。
- `scripts/check-logic.ts` に「16時時点で低温風3m以上なら18時風強まり補正は重複させない」回帰テストを追加。
- チェック数表示を `61 / 61 checks passed` に更新。

### 確認結果

- `npm run check:logic` PASS（`61 / 61 checks passed`）。
- `npm run build` PASS。
- `npm run lint` は既存の lint 指摘で失敗。今回変更範囲とは別の未使用変数・React Hooks ルール指摘が残っている。

## 2026-05-19 暖かい日の18時風強まり補正追加

ユーザー要望により、店舗が風の影響を受けやすい前提として、15時値引で18時の風が強まる場合の補正を暖かい日にも追加した。

### 仕様

- 15時値引限定。
- 18時が16度以上で、18時の風速が15時より強まり、かつ18時風速が5m以上の場合、曜日基準を `+1段` 強める。
- ただし、16時時点で通常風補正（16時が16度以上かつ風速5m以上）をすでに拾っている場合は、18時風強まり補正は追加しない。
- 18時が15度以下の場合は従来どおり、寒い日の風強まり補正（18時風速3m以上）を使う。
- 30度→24度のように18時が21〜25度へ下がる場合は、気温差では `-1段`、18時風5m以上への強まりでは `+1段` となり、両方が出る場合は差し引き0段になる。

### 実装内容

- `ResolvedWeatherInput` に `next18WindWorsenKind: 'cold' | 'warm' | null` を追加。
- `resolveWeatherInputForDiscount()` で、18時風強まりを寒い日用・暖かい日用に分岐。
- 暖かい日の18時風強まり表示を `18時が16度以上で風5m以上に強まる +1段` とした。
- `scripts/check-logic.ts` に以下の回帰テストを追加。
  - 30度→24度で18時風5m以上なら、気温差 `-1段` と暖かい日の風強まり `+1段` が両方出て相殺される。
  - 16時時点で暖かい日の風5m以上を拾った場合は、18時風強まり補正を重複させない。

### 確認結果

- `npm run check:logic` PASS（`62 / 62 checks passed`）。
- `npm run build` PASS。
- `npm run lint` は既存の lint 指摘で失敗。今回変更範囲とは別の未使用変数・React Hooks ルール指摘が残っている。

## 2026-05-19 寒い日の風5m以上を+2段に変更

ユーザー要望により、気温15度以下の寒い日の風補正を段階化した。

### 仕様

- 気温15度以下で風速3〜4mの場合：曜日基準 `+1段`。
- 気温15度以下で風速5m以上の場合：曜日基準 `+2段`。
- 気温16度以上の通常風補正は従来どおり風速5m以上で `+1段`。
- 15時→18時の寒い日の風強まり補正も同じ段階化を適用。
  - 18時が15度以下、15時より風が強まり、18時風速3〜4mなら `+1段`。
  - 18時が15度以下、15時より風が強まり、18時風速5m以上なら `+2段`。
- 16時時点で低温風3m以上をすでに拾った場合は、従来どおり18時側の寒い風強まり補正は追加しない。

### 表示文言

- `風 3〜4m（15度以下） +1段`
- `風 5m以上（15度以下） +2段`
- `18時が15度以下で風3〜4mに強まる +1段`
- `18時が15度以下で風5m以上に強まる +2段`

### 実装内容

- `getWindShift()` / `getWindShiftTerm()` で、15度以下かつ5m以上を `+2段` に変更。
- `ResolvedWeatherInput.next18WindWorsenShift` を `0 | 1 | 2` に変更。
- `resolveWeatherInputForDiscount()` で寒い日の18時風強まりが5m以上なら `+2段` を返すよう変更。
- `scripts/check-logic.ts` に「低気温 + 風速5m以上は風だけで2段強める」回帰テストを追加。
- 既存の寒い日の風強まりテスト・シナリオを新仕様に合わせて更新。

### 確認結果

- `npm run check:logic` PASS（`63 / 63 checks passed`）。
- `npm run build` PASS。
- `npm run lint` は既存の lint 指摘で失敗。今回変更範囲とは別の未使用変数・React Hooks ルール指摘が残っている。

## 2026-05-19 暖かい日の18時風強まり補正を廃止

ユーザー要望により、前回追加した「18時が16度以上で風5m以上に強まる +1段」を廃止した。
通常の強風補正は本来「値引後30分〜1時間後の風速」で見るものであり、15時→18時の風強まりは「15時から18時にかけて一気に冷え込む場合の追加悪化」だけを拾う仕様に戻した。

### 仕様

- 15時値引の通常風補正は従来どおり、16時の気温・風速で判定する。
  - 16時が15度以下・風速3〜4m：`+1段`。
  - 16時が15度以下・風速5m以上：`+2段`。
  - 16時が16度以上・風速5m以上：`+1段`。
- 15時→18時の風強まり補正は、以下をすべて満たす場合だけ適用する。
  - 15時値引である。
  - 15時→18時で6度以上下がる。
  - 18時が15度以下である。
  - 18時の風速が15時より強まる。
  - 18時風速が3m以上である。
  - 16時時点で低温風3m以上の補正を拾っていない。
- 18時が16度以上の場合は、18時風速が5m以上へ強まっても追加補正しない。

### 表示文言

- 廃止：`18時が16度以上で風5m以上に強まる +1段`
- 継続：`18時が15度以下で風3〜4mに強まる +1段`
- 継続：`18時が15度以下で風5m以上に強まる +2段`

### 実装内容

- `resolveWeatherInputForDiscount()` から暖かい日の18時風強まり判定を削除。
- `ResolvedWeatherInput.next18WindWorsenKind` を `'cold' | null` に戻した。
- `getNext18WindWorsenShiftTerm()` から暖かい日の表示分岐を削除。
- `scripts/check-logic.ts` の回帰テストを更新し、30度→24度で18時風5m以上でも気温差の `-1段` だけが入り、暖かい日の18時風強まり補正が出ないことを確認するようにした。

### 確認結果

- `npm run check:logic` PASS（`63 / 63 checks passed`）。
- `npm run build` PASS。
- `npm run lint` は既存の lint 指摘で失敗。今回変更範囲とは別の未使用変数・React Hooks ルール指摘が残っている。

## 2026-05-19 変更: 日次注意事項に「商品の減り方」を追加

- 1日1回表示される注意事項に、以下の項目を追加した。
  - `多い・少ないの判断は、残り数だけでなく商品の減り方も含める`
- 目的:
  - 「多い・少ない」の判断を、単純な残数だけでなく、ここまでの売れ方・減り方も踏まえて行うことを明記するため。
- 機能ロジックの変更はなし。

## 2026-05-19 変更: 日次注意事項の文言・太字範囲を整理

1日1回表示される箇条書きの注意事項を、流し見でも重要部分を拾いやすい表記に整理した。

### 表示文言

- `残り2個の商品は「多い」にしない`
- `多い・少ないの判断は、残り数だけでなく商品の減り方も含める`
- `少ない判定の商品は、最終値引以外では引かない`
- `見た目が悪い個別商品は、表示値引率に+10%`
- `不人気な商品は、表示値引率に+10%`
- `売れ方が順調な定番・広告商品は、表示値引率から-10%`
- `夜によく売れる商品は、表示値引率から-10%`

### 太字方針

最悪太字部分だけを見ても意味が通るよう、対象・判断・補正値を中心に `<strong>` で強調した。

- `残り2個` / `「多い」にしない`
- `多い・少ないの判断` / `商品の減り方`
- `少ない判定` / `最終値引以外では引かない`
- `見た目が悪い個別商品` / `+10%`
- `不人気な商品` / `+10%`
- `売れ方が順調な定番・広告商品` / `-10%`
- `夜によく売れる商品` / `-10%`

機能ロジックの変更はなし。


## 2026-05-19 変更: 19時売場チェックを追加

17時値引が完了しており、19:00以降になっても18時30分値引の天候入力に進んでいない場合、確認ダイアログなしで `19時売場チェック` に移行する機能を追加した。

### 仕様

- 自動移行条件:
  - 現在のセッションが17時値引である。
  - すべてのエリア確認が終わり、完了画面にいる。
  - 当日19:00以降である。
  - 19時売場チェックが未記録である。
- 18時30分値引の天候入力に進んでいる場合は、セッションが17時値引ではなくなるため自動移行しない。
- ワンクッションの確認画面は出さず、条件を満たしたら直接チェック画面を表示する。

### 入力内容

全エリアを一覧表示し、各エリアを5択で評価する。

- `減りすぎ`
- `やや減りすぎ`
- `ちょうどいい`
- `やや残りすぎ`
- `残りすぎ`

初期値はすべて `ちょうどいい`。違うエリアだけ変更して `記録して終了` を押す運用。

### 実装内容

- `src/domain/review19.ts` を追加し、19時チェックの評価値、初期値生成、正規化、自動開始判定を切り出した。
- `ScreenName` に `review19` を追加。
- `AppState` に `review19` を追加。
- `Review19Screen` を追加。
- `useNebikiApp` に以下を追加。
  - 19:00以降の自動遷移 effect。
  - `review19Items`。
  - `updateReview19Rating()`。
  - `saveReview19()`。
- `nebiki-helper/review19-records` という localStorage キーを追加し、記録済みの19時チェックを日付・セッション単位で保存するようにした。

### 確認結果

- `npm run check:logic` PASS（`63 / 63 checks passed` 表示後、既存の追加チェックも PASS）。
- `npm run build` PASS。
- `npm run lint` は既存の lint 指摘で失敗。今回追加した TypeScript build は通っている。

## 2026-05-19 変更: 19時売場チェックに値引状況スナップショットを追加

19時売場チェックの5択評価だけでは後から分析できる情報が不足するため、記録時点でアプリが保持している値引状況を `snapshot` として一緒に保存するようにした。

### 保存される主な情報

- 19時チェック本体
  - 日付
  - セッション開始時刻
  - 各エリアの5択評価
  - 記録時刻
- スナップショット
  - セッション情報
    - 日付
    - 曜日
    - 値引時刻
    - セッション開始時刻
    - 手動曜日・手動時刻指定の有無
    - 入力天候
    - 解決後の天候区分
  - 曜日基準・補正情報
    - 元の曜日基準
    - 補正後の曜日基準
    - 曜日基準補正段数
    - 値引率補正
    - 時刻接近による補正
    - 補正理由・計算表示
  - エリア別情報
    - エリア名
    - 完了・未完了・手動スキップ・少ない後回し・前回+5%済み自動スキップなどの状態
    - 17時時点のエリア判定
    - 完了時または自動スキップ時の表示値引率
    - 多い用値引率
    - 通常値引率
    - 表示メモ
    - 訪問時刻・完了時刻
    - スキップ理由

### 追加調整

- エリア完了時に、その時点で表示していた値引率を `AreaProgress` に保存するようにした。
  - 19時チェック時点で実時間補正が変わっていても、完了時に見せた値引率を後から参照できるようにするため。
- 前回 `次の値引の方が近くなったため+5%` で完遂したエリアを次回自動スキップする場合、次回スキップ予約にも前回表示値引率を保持するようにした。
- 自動スキップしたエリアは、案内と完了画面の値引率表示で前回値引率を表示できるようにした。

### 注意

- 19時チェックに保存するスナップショットは、記録時点で保持している現在セッション中心の情報。
- 15時セッションの詳細履歴は、現時点では別途アーカイブしていない。
- 15時値引の良し悪しは、17時のエリア判定（多い・どちらでもない・少ない）から推定する運用を想定している。

### 確認結果

- `npm run check:logic` PASS（`64 / 64 checks passed` 表示後、既存の追加チェックも PASS）。
- `npm run build` PASS。
- `npm run lint` は既存の lint 指摘で失敗。今回追加した `scripts/check-logic.ts` の lint 指摘は修正済み。

## 2026-05-22 変更: 19時売場チェック前に19時30分用の天候入力を挟む

19時売場チェックは、その時点の絶対量だけではなく「19時30分値引の基準から見て適切か」を判断する必要があるため、振り返り前に19時30分用の天候入力を行う流れに変更した。

### 仕様

- 17時値引完了後、以下をすべて満たす場合に自動で19時売場チェック用の天候入力へ移行する。
  - 17時値引が完了済み。
  - 画面が完了画面。
  - 18時30分値引の天候入力へ進んでいない。
  - その日の19時売場チェックが未記録。
  - 現在時刻が19:15以降。
- 19時売場チェックの前に、19時30分値引用の天候を入力する。
- 天候入力後、確認画面を挟まずに19時売場チェックへ進む。
- 19時売場チェック画面には、入力した19時30分基準の曜日基準・補正概要を表示する。
- 実際の値引セッションは17時のまま保持し、19時30分値引を開始した扱いにはしない。
- 保存する19時チェックには、19時30分用天候・解決後天候・曜日基準補正情報を `reference` および `snapshot.reviewReference` として残す。

### 実装内容

- `ScreenName` に `review19_weather` を追加。
- `Review19Result` に `reference` を追加。
- `Review19Snapshot` に `reviewReference` を追加。
- `shouldAutoStartReview19()` の開始時刻を19:00以降から19:15以降へ変更。
- `useNebikiApp` に以下を追加。
  - 19時売場チェック用の19時30分天候入力画面への自動遷移。
  - `startReview19AfterWeather()`。
  - `review19ReferenceLines`。
- `Review19Screen` に19時30分値引の基準表示を追加。

### 確認結果

- `npm run check:logic` PASS（`65 / 65 checks passed` 表示後、既存の追加チェックも PASS）。
- `npm run build` PASS。
- `npm run lint` は既存 lint 指摘で失敗。今回変更範囲とは別の未使用変数・React Hooks 系の指摘が残っている。

## 2026-05-22 変更: 19時売場チェック完了画面と10日分データ出力

19時売場チェック終了後は値引作業の完了ではなく振り返りデータの保存完了であるため、通常の値引完了画面とは別の専用画面へ移行するようにした。

### 仕様

- 19時売場チェックで「記録して終了」を押した後、`done` ではなく `review19_done` へ移行する。
- `review19_done` では「19時売場チェックを記録しました」と表示し、通常の「すべてのエリアの確認が終わりました」画面とは区別する。
- 19時売場チェックの保存データには `exportedAt` を持たせ、未出力データと出力済みデータを区別する。
- 未出力の19時売場チェックデータが10日分以上ある場合、振り返り完了画面に「10日分のデータを出力」ボタンを表示する。
- ボタンを押すと、古い順に10日分を `nebiki-review19-開始日_終了日.json` として保存する。
- 保存後、対象10日分に `exportedAt` を付けて出力済みにする。
- 未出力が10日未満の場合は、未出力日数と「10日分たまると出力できる」旨だけを表示する。

### 実装内容

- `ScreenName` に `review19_done` を追加。
- `Review19Result` に `exportedAt` を追加。
- `Review19DoneScreen` を追加。
- `review19.ts` に以下を追加。
  - `REVIEW19_EXPORT_BATCH_SIZE`
  - `getUnexportedReview19Records()`
  - `getReview19ExportBatch()`
  - `buildReview19ExportPayload()`
  - `markReview19RecordsExportedInMemory()`
- `useNebikiApp` に以下を追加。
  - 未出力件数の算出。
  - `exportReview19Records()`。
  - 19時売場チェック保存後の `review19_done` 画面移行。
- 10日分の未出力データを古い順に出力し、出力済みにできる回帰チェックを追加。

### 確認結果

- `npm run check:logic` PASS（`65 / 65 checks passed` 表示後、既存の追加チェックと今回追加チェックも PASS）。
- `npm run build` PASS。
- `npm run lint` は既存 lint 指摘で失敗。今回変更範囲とは別の未使用変数・React Hooks 系の指摘が残っている。

## 2026-05-22 変更: 19時売場チェック出力用データの分析項目を補強

19時売場チェックの出力データを後から分析しやすくするため、5択評価の点数と値引率の数値フィールドを追加した。

### 仕様

- 19時売場チェックの各エリア評価は、文字列の評価名に加えて点数も保存する。
  - `decreased_too_much`（減りすぎ）: `-2`
  - `decreased_slightly_too_much`（やや減りすぎ）: `-1`
  - `just_right`（ちょうどいい）: `0`
  - `remained_slightly_too_much`（やや残りすぎ）: `1`
  - `remained_too_much`（残りすぎ）: `2`
- `Review19Result.ratingScores` として、エリア別の点数を保存・出力する。
- 19時チェックのスナップショット内エリア情報に、表示値引率の数値も保存する。
  - `ratePercent`
  - `manyRatePercent`
  - `normalRatePercent`
- `30%` のような表示は `30` として保存する。
- `引かない` は `0` として保存する。
- `未完了` や `前回値引率不明` のように値引率数値を取れない表示は、数値フィールドを持たせない。
- 既存保存データを読み込む場合も、評価点数と値引率数値は可能な範囲で正規化して補完する。

### 実装内容

- `Review19RatingScore` 型を追加。
- `REVIEW19_RATINGS` に `score` を追加。
- `createReview19RatingScores()` を追加し、評価文字列からエリア別点数を生成するようにした。
- `parseReview19RatePercent()` を追加し、表示値引率文字列から数値を抽出するようにした。
- `Review19AreaSnapshot` に数値フィールドを追加。
- `normalizeReview19Result()` で、古い19時チェックデータにも `ratingScores` と値引率数値を補完するようにした。
- 19時チェック保存時に、最新の評価点数と値引率数値を一緒に保存するようにした。
- スナップショット・点数・値引率数値が保持される回帰チェックを追加・更新。

### 確認結果

- `npm run check:logic` PASS（`65 / 65 checks passed` 表示後、既存の追加チェックも PASS）。
- `npm run build` PASS。
- `npm run lint` は既存 lint 指摘で失敗。今回変更範囲とは別の未使用変数・React Hooks 系の指摘が残っている。

## 2026-05-24 変更: 19時売場チェックの対象外エリアと手動開始ボタン

製造量が少ない日のデータを通常の振り返りに混ぜると、値引の強弱ではなく製造量の少なさを「減った」と誤解しやすいため、15時値引で「少ない」を選んだエリアは19時売場チェックの評価対象から外すようにした。

また、19:15以降に自動で19時売場チェックへ入らない場合に備え、17時値引完了画面から手動で19時売場チェックへ入れるボタンを追加した。

### 仕様

- 15時値引で「少ない」を選んだエリアは、当日の19時売場チェックでは5択評価の対象外にする。
- 対象外エリアは19時売場チェック画面に `対象外：15時に「少ない」判定` と表示し、評価ボタンは表示しない。
- 出力データにも以下を保存する。
  - `excludedAreaIds`
  - `excludeReasons`
  - スナップショット内の `reviewExcluded`
  - スナップショット内の `reviewExcludeReason`
- 19時売場チェックの自動開始条件で、同日の未記録チェック下書きが残っていても、19:15以降なら再び振り返りへ入れるようにした。
- 17時値引完了画面に `19時売場チェックに入る` ボタンを追加した。
  - 17時値引が完了済み、かつ当日の19時売場チェックが未記録の場合だけ表示する。
  - 押すと19時30分値引用の天候入力画面へ進む。

### 実装内容

- `AppState` に `review19ExcludedAreaIds` を追加。
- 15時値引で `few` を選んだエリアを `review19ExcludedAreaIds` に保持するようにした。
- 15時から17時へ値引帯移動する際も、15時の `few` 判定エリアを保持するようにした。
- `Review19Result` に `excludedAreaIds` と `excludeReasons` を追加。
- `Review19AreaSnapshot` に `reviewExcluded` と `reviewExcludeReason` を追加。
- `Review19Screen` で対象外エリアは評価ボタンではなく対象外理由を表示するようにした。
- `DoneScreen` に手動開始ボタンを追加。
- `shouldAutoStartReview19()` は、同日未記録の振り返り下書きが残っていても19:15以降なら自動開始できるようにした。
- 15時少ない判定エリアの対象外保存と、未記録下書きがある場合の自動開始について回帰チェックを追加。

### 確認結果

- `npm run check:logic` PASS（`66 / 66 checks passed` 表示後、既存の追加チェックも PASS）。
- `npm run build` PASS。
- `npm run lint` は既存 lint 指摘で失敗見込み。今回変更範囲とは別の未使用変数・React Hooks 系の指摘が残っている。

## 2026-05-24 変更: 19時売場チェック対象外を「15時・17時ともに少ない」に修正

前回版では、15時値引で「少ない」を選んだエリアをそのまま19時売場チェック対象外にしていたが、15時に少なくても17時で「どちらでもない」または「多い」に復活したエリアは通常の振り返り対象に戻すべきだったため、対象外条件を修正した。

### 仕様

- 19時売場チェックで対象外にするのは、15時値引と17時値引の両方で「少ない」判定だったエリアだけ。
- 15時で「少ない」だったエリアでも、17時で「どちらでもない」または「多い」に復活した場合は19時売場チェックの5択評価対象にする。
- 対象外表示は `対象外：15時・17時ともに「少ない」判定` とする。
- 出力データの対象外理由も、新規データでは `few_at_15_and_17` を使う。
- 旧データの `few_at_15` は読み込み時に `few_at_15_and_17` と同じ意味へ正規化する。

### 実装内容

- `Review19ExcludeReason` に `few_at_15_and_17` を追加。
- `createInitialReview19Result()` で作る対象外理由を `few_at_15_and_17` に変更。
- 17時値引で、15時「少ない」候補だったエリアを「どちらでもない」または「多い」にした場合は、19時チェック対象外候補から外すようにした。
- 19時売場チェック開始時は、`review19ExcludedAreaIds` をそのまま使わず、17時側の最新 `areaJudge` も確認し、17時も `few` のエリアだけ対象外にするようにした。
- 19時チェックのスナップショットにも、対象外理由として `few_at_15_and_17` を保存するようにした。
- 回帰チェック文言を「15時・17時ともに少ない判定したエリアは19時チェック対象外として保存できる」に更新。

### 確認結果

- `npm run check:logic` PASS（`66 / 66 checks passed` 表示後、既存の追加チェックも PASS）。
- `npm run build` PASS。
- `npm run lint` は既存 lint 指摘で失敗。今回変更範囲とは別の未使用変数・React Hooks 系の指摘が残っている。

## 2026-05-24 変更: 19時売場チェック手動開始ボタンの表示条件を補強

20:55時点で `19時売場チェックに入る` ボタンが見当たらないケースがあったため、表示条件と時刻切替処理を見直した。

### 原因

- 手動開始ボタンは `done` 画面にだけ表示していた。
- 17時値引完了後、19:15以降に通常の時刻切替処理が先に走ると、画面が `start` に戻ることがあり、その場合は `done` 画面ではないため手動開始ボタンが表示されなかった。
- 自動19時売場チェックと、17時値引から後続値引へ進む時刻切替の優先順位が分かりにくかった。

### 仕様

- 19時売場チェックを開始できる条件を共通化した。
  - 当日の17時値引セッションがある。
  - 17時値引の全エリアが未開始ではない状態まで進んでいる。
  - 当日の19時売場チェックが未記録。
  - 現在時刻が19:15以降。
- 上記を満たす場合は、`done` 画面だけでなく `start` 画面でも `19時売場チェックに入る` ボタンを表示する。
- 19時売場チェックを開始できる状態では、17時値引から18:30/19:30/20:30値引へ進む時刻切替確認を出さない。
  - 19:15以降は、17時値引完了後の振り返りを優先する。

### 実装内容

- `useNebikiApp` に `canStartReview19FromCurrentState()` と `hasNoUnstartedReviewAreas()` を追加。
- `canStartReview19Manually` を `done` 画面限定から、条件を満たす `done` / `start` 画面対応に変更。
- `startReview19Manually()` も `done` / `start` 画面から開始できるように変更。
- 通常の時刻切替 `useEffect` は、19時売場チェック開始可能状態なら何もしないように変更。
- `StartScreen` に `canStartReview19` / `onStartReview19` を渡せるようにし、`start` 画面にも手動開始ボタンを表示できるようにした。

### 確認結果

- `npm run check:logic` PASS（`66 / 66 checks passed` 表示後、既存の追加チェックも PASS）。
- `npm run build` PASS。
- `npm run lint` は既存 lint 指摘で失敗見込み。今回変更範囲とは別の未使用変数・React Hooks 系の指摘が残っている。

## 2026-05-24 変更: 値引セッション状態を再読み込み・タブ閉じ後も復元できるように強化

ページ更新やタブを閉じた後に、17時値引完了状態・+5%次回スキップ予約・19時売場チェック対象状態などが失われると実運用で危険なため、値引中の状態復元を強化した。

### 仕様

- アプリ操作中の現在状態は従来通り `nebiki-helper/current-session` に保存する。
- それに加えて、値引セッションが存在する状態は `nebiki-helper/work-session-checkpoint` にチェックポイントとして保存する。
- ページ更新後、通常の現在状態が開始画面・セッションなしに戻っていても、同日の値引チェックポイントが残っていればそこから自動復元する。
- `nextSessionSkipRecords` は従来通り保存されるため、+5%値引済みエリアの次回自動スキップ予約も再読み込み後に残る。
- 戻る履歴、直前操作の取り消し、編集中の復帰先などの一時的な操作状態も `nebiki-helper/runtime-state` に保存する。
- `リセット` 操作をした場合は、作業チェックポイントと一時操作状態を明示的に削除する。

### 実装内容

- `src/domain/storage.ts` に以下を追加。
  - `workSessionCheckpoint`
  - `runtimeState`
  - `loadWorkSessionCheckpoint()` / `saveWorkSessionCheckpoint()` / `clearWorkSessionCheckpoint()`
  - `loadRuntimeState()` / `saveRuntimeState()` / `clearRuntimeState()`
- `useNebikiApp` の初期化時、同日の作業チェックポイントがあり、現在状態が開始画面・セッションなしの場合は、チェックポイントを優先して復元するようにした。
- `useNebikiApp` で `state.session` が存在する状態は作業チェックポイントにも保存するようにした。
- `areaJudgeSelection`、`resumeTargetScreen`、`timeSwitchTarget`、`undoSnapshot`、`screenHistory` を runtime-state に保存し、再読み込み後に復元するようにした。
- `resetApp()` で作業チェックポイントと runtime-state を削除するようにした。

### 確認結果

- `npm run check:logic` PASS（`66 / 66 checks passed` 表示後、既存の追加チェックも PASS）。
- `npm run build` PASS。
- `npm run lint` は既存 lint 指摘で失敗見込み。今回変更範囲とは別の未使用変数・React Hooks 系の指摘が残っている。

## 2026-05-24 追記: エージェント機能の利用提案について

ユーザーは現状、通常のチャットで仕様相談 → ZIP修正 → 実機確認 → 追加修正という進め方を基本にしている。通常の修正・仕様整理・ZIP更新は、引き続きこのスタイルで進めてよい。

ただし、作業量や確認範囲が大きくなり、通常チャットだけでは非効率になりそうな場合は、ChatGPT側からエージェント機能の利用を提案すること。

この方針は、単なる備考ではなく **作業開始時に毎回確認するチェック項目** として扱う。ZIP修正や実装に入る前に、今回の依頼が下記の提案対象に当てはまるかを確認すること。

### 作業開始時の提案判定

ChatGPT は作業開始時に、今回の依頼が以下に当てはまるか確認する。
当てはまる場合は、ZIP修正・実装・分析に入る前に、短く一度だけ「これはエージェント機能や Codex を使った方がよい可能性があります」と提案する。

### 提案対象

- 複数機能にまたがる大きな設計変更。
- 状態保存・復元・localStorage など、アプリ全体に影響する監査。
- 既存実装の抜け漏れを広範囲に洗い出す作業。
- lint 指摘の整理、リファクタリング、テスト拡充など広範囲のコード整理。
- GitHub / Vercel / 実ブラウザ確認など外部環境を含む作業。
- 10日分以上の振り返りデータ分析やレポート化。
- デプロイ後の画面確認、操作確認、回帰確認など反復作業が多い場合。
- ユーザーが「全体を見直して」「漏れがないか確認して」「安全に整理して」「全部洗い出して」「実際に操作して確認して」「データをまとめて分析して」など、範囲が広い依頼をした場合。

### 通常チャットで十分な作業

- 単発の文言修正。
- 小さな仕様変更。
- 1〜2箇所のロジック修正。
- ユーザーと相談しながら仕様を詰める段階。
- ZIP内の `CHATGPT_HANDOFF.md` だけを更新する作業。

### 提案の仕方

- エージェント機能を無理に勧めない。
- 通常チャットで十分進められる場合は、従来どおり進める。
- 提案する場合は、「この作業は画面操作や横断確認が多いので、エージェント機能を使うと楽になる可能性があります」程度に短く伝える。
- コード作業・全体監査・リファクタリングが中心なら Codex が候補になる。ブラウザ操作・外部サービス確認・データ分析・資料化などを含む場合は、より広い意味で ChatGPT のエージェント機能が候補になる。
- 提案はあくまで選択肢として出すだけで、最終判断はユーザーに委ねる。


## 2026-05-25 変更: 19時30分開始時に18時30分未実施なら19時売場チェックへ差し込む

19時売場チェックの自動開始や手動ボタンが、再読み込み・画面状態によって見つけにくいケースがあったため、実際の操作導線に合わせて追加の入口を作った。

- 19時30分値引の天候入力後に「値引を始める」ボタンを押した時、以下をすべて満たす場合は19時30分値引を開始せず、19時売場チェックへ直接入る。
  - 直前の値引セッションが17時値引である。
  - 19時30分値引を開始しようとしている。
  - 18時30分値引が開始されていない。
  - 17時値引の全エリアが未開始ではない状態まで進んでいる。
  - 当日の19時売場チェックがまだ記録済みではない。
- この場合、すでに入力した19時30分用の天候を、そのまま19時売場チェックの基準として使う。
- 18時30分値引を開始済みの場合は、従来どおり19時30分値引へ進み、19時売場チェックには差し込まない。
- 回帰チェックとして「18時30分未開始で19時30分値引を始めようとしたら19時チェックへ差し込む」を追加した。

## 2026-05-25 変更: 気温補正を天候ポイント制に一本化

夜21〜25度の日は夜も売れ伸びるという現場感を反映するため、従来の単発の気温帯補正と `15時→18時` の気温差補正を廃止し、16時以降の気温を合計する **天候ポイント制** に一本化した。

この変更は、通常の気温補正・15時→18時の快適化/冷え込み補正を置き換えるもの。風補正、雨・雪補正、GW補正、時刻接近補正は従来通り別枠で扱う。

### 入力仕様

- 天候入力の時刻表示は `16時〜21時` に変更。
- 15時値引では `16〜21時` を入力対象にする。
- 17時値引では `18〜21時`、18時30分値引では `19〜21時`、19時30分値引では `20〜21時` を入力対象にする。
- `15時` の天候入力欄は表示しない。
- 20時30分値引は従来通り個数固定で、天候ポイントによる値引方法の変更は行わない。

### 天候ポイント

各入力対象時刻の気温を以下の点数に変換し、合計する。

| 気温 | 点数 |
|---:|---:|
| 5度以下 | -2pt |
| 6〜10度 | -1pt |
| 11〜15度 | 0pt |
| 16〜20度 | +1pt |
| 21〜25度 | +2pt |
| 26〜27度 | +1pt |
| 28〜30度 | 0pt |
| 31〜35度 | -1pt |
| 36度以上 | -2pt |

### 曜日基準への反映

| 合計点 | 補正 |
|---:|---:|
| +7pt以上 | 曜日基準を2段緩める |
| +4〜+6pt | 曜日基準を1段緩める |
| -3〜+3pt | 補正なし |
| -4〜-6pt | 曜日基準を1段強める |
| -7pt以下 | 曜日基準を2段強める |

プラスは「売れやすいので値引を抑える」、マイナスは「売れにくいので値引を強める」という意味。

表示例：

- `天候ポイント +9pt（16〜21時） -2段`
- `天候ポイント -7pt（16〜21時） +2段`

### 旧仕様からの変更点

- 旧 `気温 21〜25度 -2段` などの単発気温帯補正は廃止。
- 旧 `15時と18時の気温差が6度以上` 補正は廃止。
- 旧 `18時が15度以下で風が強まる` 補正も廃止。
- 低温・高温・夜の快適化・冷え込みはすべて天候ポイントで表現する。
- 風補正は引き続き「30分〜1時間後の風速」で見る。
  - 15度以下・風3〜4m: +1段
  - 15度以下・風5m以上: +2段
  - 16度以上・風5m以上: +1段

### 実装内容

- `ResolvedWeatherInput` に `weatherPointScore` / `weatherPointShift` / `weatherPointRangeText` を追加。
- `resolveWeatherInputForDiscount()` で、値引時刻ごとの入力対象時刻から天候ポイントを計算するよう変更。
- `weekdayBase.ts` では `getTempShift()` 系と `getNext18...` 系を曜日基準計算から外し、`weatherPointShift` を使用するよう変更。
- `StartScreen` の天候入力表示を `16〜21時` 起点に変更。
- `scripts/check-logic.ts` の天候系回帰テストを天候ポイント制に合わせて更新。

### 確認結果

- `npm run check:logic` PASS。
- `npm run build` PASS。
- `npm run lint` は既存 lint 指摘で失敗見込み。今回変更範囲とは別の既存指摘が残っている。

## 2026-05-25 変更: 天候補正を「直近ベース + 未来天候ポイント」に再整理

直前の「天候ポイント制に一本化」からさらに整理し、天候補正を以下の2層に分けた。

1. **30分〜1時間後の天候**で、その値引直後の売れ方を見る「ベース」を作る。
2. **1時間30分〜2時間後以降の天候**を、未来天候ポイントとして合計し、ベースに対して曜日基準を加減算する。

これにより、「本来は30分〜1時間後の天候を見て値引する。ただし、その後の気温・雨・雪・風の変化も織り込む」という運用意図に合わせた。

### 直近ベース

値引時刻ごとの30分〜1時間後の時刻を直近ベースとして扱う。

| 値引 | 直近ベース |
|---|---|
| 15時 | 16時 |
| 17時 | 18時 |
| 18時30分 | 19時 |
| 19時30分 | 20時 |
| 20時30分 | 21時 |

直近ベースでは以下を従来どおり直接反映する。

- 気温帯による曜日基準補正
  - 5度以下: +2段
  - 6〜10度: +1段
  - 11〜15度: 0段
  - 16〜20度: -1段
  - 21〜25度: -2段
  - 26〜27度: -1段
  - 28〜30度: 0段
  - 31〜35度: +1段
  - 36度以上: +2段
- 風速による曜日基準補正
  - 15度以下・風3〜4m: +1段
  - 15度以下・風5m以上: +2段
  - 16度以上・風5m以上: +1段
- 直近の雨・雪による値引率補正
  - 雨: +10%
  - 雪: +20%

### 未来天候ポイント

直近ベースより後の時刻を未来天候ポイントとして扱う。

| 値引 | 未来天候ポイント対象 |
|---|---|
| 15時 | 17〜21時 |
| 17時 | 19〜21時 |
| 18時30分 | 20〜21時 |
| 19時30分 | 21時 |
| 20時30分 | なし |

未来天候ポイントでは、各時刻ごとに **気温・雨/雪・風** を点数化し、合計する。

#### 気温ポイント

| 気温 | 点数 |
|---:|---:|
| 5度以下 | -2pt |
| 6〜10度 | -1pt |
| 11〜15度 | 0pt |
| 16〜20度 | +1pt |
| 21〜25度 | +2pt |
| 26〜27度 | +1pt |
| 28〜30度 | 0pt |
| 31〜35度 | -1pt |
| 36度以上 | -2pt |

#### 雨・雪ポイント

| 天候 | 点数 |
|---|---:|
| 晴れ | 0pt |
| 雨 | -1pt |
| 雪 | -2pt |

#### 風ポイント

| 気温・風速 | 点数 |
|---|---:|
| 15度以下・風3〜4m | -1pt |
| 15度以下・風5m以上 | -2pt |
| 16度以上・風5m以上 | -1pt |
| 上記以外 | 0pt |

#### 合計点の曜日基準反映

| 合計点 | 補正 |
|---:|---|
| +7pt以上 | 曜日基準を2段緩める |
| +4〜+6pt | 曜日基準を1段緩める |
| -3〜+3pt | 補正なし |
| -4〜-6pt | 曜日基準を1段強める |
| -7pt以下 | 曜日基準を2段強める |

表示例は以下のようにした。

- `16時気温 21〜25度 -2段`
- `風 5m以上（15度以下） +2段`
- `未来天候ポイント +9pt（17〜21時） -2段`
- `未来天候ポイント -7pt（17〜21時） +2段`

### 旧仕様からの変更点

- 直前の「天候ポイント制に一本化」では、直近時刻も未来時刻も同じ天候ポイントに入れていた。
- 今回の変更で、直近時刻はベース、以降の時刻は未来天候ポイントとして分離した。
- 後続時間の雨・雪を単独で `+1段/+2段` する旧仕様は廃止し、未来天候ポイントの雨・雪点として扱う。
- 未来の風も、単独補正ではなく未来天候ポイントの風点として扱う。
- 20時30分値引は従来どおり個数固定で、未来天候ポイント対象なし。

### 確認結果

- `npm run check:logic`: PASS（47 / 47 checks passed。加えて後続の個別チェックもPASS）
- `npm run build`: PASS
- `npm run lint`: 既存 lint 指摘で失敗。今回の TypeScript build は通過済み。

## 2026-05-25 変更: 未来天候ポイントの低温側を調整
ユーザー要望により、未来天候ポイントの気温低温側を以下へ変更した。

| 未来時刻の気温 | ポイント |
|---:|---:|
| 5度以下 | -2pt |
| 6〜10度 | -1pt |
| 11〜15度 | 0pt |

意図:
- 11〜15度は、気温単体では未来天候ポイントのマイナスにしない。
- 11〜15度でも雨・雪・風がある場合は、それぞれの未来天候ポイントで悪条件として拾う。
- 6〜10度は軽い冷え込みとして -1pt、5度以下は強い冷え込みとして -2pt。

確認:
- 未来が11〜15度だけなら、未来天候ポイントによる曜日基準補正は出ない。
- 未来が6〜10度だけ続く場合は -5pt（15時・17〜21時）で1段強める。
- 未来が5度以下を含んで強く冷え込む場合は、従来どおり2段強める条件に届く。

## 2026-05-26 変更: 19時売場チェック導線と値引開始可能時刻の修正

ユーザー報告で、19時売場チェックへ入れないケースが続いたため、19時30分値引開始時の導線をより確実にした。

### 19時30分値引前の振り返り導線

- 19時30分値引は、原則として **19時売場チェックへ入ることをデフォルト** にした。
- 19時30分値引の天候入力後に「値引を始める」を押した時、以下の場合は19時30分値引を直接開始せず、19時売場チェックへ入る。
  - 19時30分値引を開始しようとしている。
  - 当日の19時売場チェックがまだ記録済みではない。
  - 18時30分値引を開始済みではない。
  - 17時セッションが残っている場合は同日で、かつ何らかのエリア進行がある。
  - 再読み込みなどで17時セッションが残っていない場合でも、19時30分値引前の振り返りへ入れる。
- 18時30分値引を開始済みの場合は、従来どおり19時30分値引へ進み、19時売場チェックは差し込まない。
- 19時売場チェック画面には、`記録して19時30分の値引に進む` ボタンを追加した。
  - 押すと19時チェックを保存し、そのまま19時30分値引を開始する。
- 19時売場チェック完了画面にも、`19時30分の値引に進む` ボタンを追加した。
  - いったん `記録して終了` した後でも、続けて19時30分値引へ進める。

### 値引開始時刻・天候入力開始時刻

自動判定で各値引へ切り替わる時刻を以下に変更した。

| 値引 | 自動判定で開始できる時刻 |
|---|---|
| 15時値引 | 14:40以降は15時値引扱い（アプリ上はそれ以前も15時枠が既定） |
| 17時値引 | 16:40以降 |
| 18時30分値引 | 18:10以降 |
| 19時30分値引 | 19:10以降 |
| 20時30分最終値引 | 20:15以降 |

これに合わせて、17時値引・18時30分値引の打ち切り境界と時刻接近 `+5%` の範囲も調整した。

- 17時値引の時刻接近 `+5%`: 18:00〜18:09
- 17時値引から18時30分値引への切り替え: 18:10以降
- 18時30分値引の時刻接近 `+5%`: 19:00〜19:09
- 18時30分値引から19時30分値引への切り替え: 19:10以降
- 19時30分値引の時刻接近 `+5%`: 20:00〜20:14
- 19時30分値引から20時30分最終値引への切り替え: 20:15以降

19時売場チェックの自動開始・手動開始の時刻も、19:15以降から **19:10以降** に変更した。

### 確認結果

- `npm run check:logic`: PASS（48 / 48 checks passed。加えて後続の個別チェックもPASS）
- `npm run build`: PASS
- `npm run lint`: 既存 lint 指摘で失敗。今回変更範囲とは別の既存指摘が残っている。

## 2026-05-26 修正: 19時売場チェック導線の再整理と値引開始時刻の差し戻し

直前の「19時30分値引前の振り返り導線」変更は、ユーザー意図と一部ズレていたため修正した。

### 19時30分値引の入口

- 19時30分値引へ直接入る入口は作らない。
- 19時30分値引を始めようとした場合、当日の19時売場チェックが未記録なら、まず19時売場チェックへ入る。
- 18時30分値引を開始済みの場合も、19時30分値引へ直接進めず、いったん19時売場チェックへ入る。
- 19時30分値引へ進む操作は、19時売場チェック画面または19時売場チェック完了画面の `19時30分の値引に進む` ボタンから手動で行う。
- 19時売場チェック画面のボタンは以下の2択に分けた。
  - `記録して終了`: 19時売場チェックを保存して専用完了画面へ移動する。
  - `19時30分の値引に進む`: 19時チェックを保存せず、入力済みの19時30分天候を使って19時30分値引へ進む。
- 直前版にあった `記録して19時30分の値引に進む` は廃止した。

### 値引開始時刻・時刻接近補正の差し戻し

直前版で一時的に早めた値引開始可能時刻は、ユーザー要望により元に戻した。

- `resolveDiscountTime()` の自動判定は直前版以前の時刻へ戻した。
  - 15時値引: 16:30より前
  - 17時値引: 16:30〜18:14
  - 18時30分値引: 18:15〜19:14
  - 19時30分値引: 19:15〜20:14
  - 20時30分最終値引: 20:15以降
- StartScreen 側の初期選択も直前版以前の判定へ戻した。
  - 15時値引: 16:30より前
  - 17時値引: 16:30〜18:14
  - 18時30分値引: 18:15〜19:29
  - 19時30分値引: 19:30〜20:29
  - 20時30分最終値引: 20:30以降
- 時刻接近 `+5%` の範囲も直前版以前へ戻した。
  - 17時値引: 18:00〜18:14
  - 18時30分値引: 19:00〜19:14
  - 19時30分値引: 20:00〜20:14

### 19時売場チェックの開始時刻

- 19時売場チェックだけは、19:15以降ではなく **19:00以降** に入れるようにした。
- 17時値引完了後、同日の19時売場チェックが未記録で、19:00以降なら自動開始対象になる。

### 確認結果

- `npm run check:logic`: PASS（48 / 48 checks passed。加えて後続の個別チェックもPASS）
- `npm run build`: PASS
- `npm run lint`: 既存 lint 指摘で失敗。今回変更範囲とは別の既存指摘が残っている。

## 2026-05-26 追記：nebiki-helper(8).zip の軽量化確認

ユーザーアップロード版 `nebiki-helper(8).zip` には、配布ZIPに含めない方針の `.git/`、`node_modules/`、`dist/`、`tsc*.txt` が含まれていた。

今回、配布用ZIPは以下を除外して作り直した。

- `.git/`
- `node_modules/`
- `dist/`
- `.env` 系
- `tsc*.txt` などの一時ログ

確認結果:

- 初回の `npm run build` は、同梱 `node_modules` の実行権限と Linux 用 optional native dependency 不足により失敗。
- `node_modules` を削除して `npm ci` し直した後、`npm run check:logic` PASS（48 / 48 checks passed。加えて後続の個別チェックもPASS）。
- `npm run build` PASS。
- `npm ci` 時に `npm audit` 警告（5 vulnerabilities: 2 moderate, 3 high）は出るが、今回の軽量化範囲では未対応。

## 2026-06-03 19時売場チェックの天候入力廃止

- 19時売場チェックへ入る時に、19時30分用の天候入力画面を挟まないように変更。
- トップ画面の「19時売場チェックに入る」ボタンを押すと、直接 `review19` 画面へ進む。
- 売場チェック画面に表示する「19時30分値引の基準」は、17時値引時点で入力した天気予報を `discountTime: "19"` として読み替え、`createReview19Reference(createReview19WeatherDraft(session))` で作る。
- 目的は「19時に再入力した天候」ではなく、「17時時点の予報に即した値引ができていたか」を振り返ること。
- 旧データ互換のため `review19_weather` は型とルーター上には残しているが、正規導線では使わない。旧状態で復元された場合も `normalizeLoadedState` で `review19` に寄せ、天候入力画面を表示しない。

## 2026-06-07 追記：雨雪の直接補正を「起点時刻」方式へ変更

ユーザー要望により、雨雪の直接値引率補正を、単純な対象時間帯の雨雪回数ではなく、値引直後の起点時刻を見てから継続・再発を確認する方式に変更した。

### 新しい直接補正ルール

- 起点時刻が雨ではない場合は、後続時刻が雨でも直接値引率補正にはしない。
- 起点時刻が雨なら `+5%`。
- 起点時刻が雨で、後続確認枠のどちらかも雨なら `+10%`。
- 起点時刻が雪なら `+20%`。
- 起点以外の雪は直接値引率補正にはしない。未来天候ポイント側で扱う。

### 起点と後続確認枠

- 15時値引: 起点16時、後続17時・18時
- 17時値引: 起点18時、後続19時・20時
- 18時30分値引: 起点19時、後続20時・21時
- 19時30分値引: 起点20時、後続21時・仮想22時

19時30分の仮想22時は、20時が雨なら雨、20時が雪なら雪、20時が晴れ/くもりなら雨雪なしとして扱う。

例:

- 17時値引で18時くもり・19時雨・20時雨 → 直接補正なし
- 17時値引で18時雨・19時くもり・20時雨 → `+10%`
- 19時30分値引で20時雨・21時くもり → 仮想22時も雨扱いなので `+10%`

変更ファイル:

- `src/domain/hourlyWeather.ts`
- `src/domain/weekdayBase.ts`
- `scripts/check-logic.ts`

確認結果:

- `npm run check:logic` PASS。主要チェックは `52 / 52 checks passed`。
- `npm run build` PASS。

## 2026-06-07 追記：17時の「10個以上35%」について

現行仕様では、17時値引で雨などの直接補正 `+5%` が入り、かつ通常エリア内の「多い」商品のうち10個以上の目安を表示する場合、`10%（17時基本） + 5%（雨） + 10%（多い） + 10%（10個以上） = 35%` となる。

これは「雨雪補正中は時刻別上限を外す」仕様があるため、コード上は仕様通り。
ただし運用上強すぎると判断する場合は、今後「+5%雨だけは17時30%上限を残す」「10個以上補足だけは時刻別上限を残す」などの別調整が必要。

## 2026-06-13 変更: 注意事項に残り1個商品の扱いを追加

値引率表示画面下部と日次注意事項で使う `NoticeItems` の先頭に、以下の注意事項を追加した。

- `残り1個の商品は「少ない」にする`

既存の `残り2個の商品は「多い」にしない` より上に表示し、残り1個は少ない扱いにする判断を明確化した。

確認:
- `npm run check:logic` PASS
- `npm run build` PASS

## 2026-06-22 エリア残数判定の5段階化

今回の修正で、エリア残数判定を通常値引の全時刻（15時 / 17時 / 18時30分 / 19時30分）に拡張した。20時30分は最終値引のため対象外。

エリア残数判定は、利用者の感覚ではなくアプリ側が5段階で出す方針に変更した。

- 多い: 表示値引率 +10%
- やや多い: 表示値引率 +5%
- 普通: 補正なし
- やや少ない: 表示値引率 -5%
- 少ない: 表示値引率 -10%

注意: エリア判定の「少ない」はエリア全体の-10%補正であり、従来の手動「少ない」後回しとは別物。エリア残数判定がreadyの場合は、5段階結果を固定して rate_display に進む。

比較条件は以下。

- 同じエリア
- 同じ値引時刻
- 同じ実際の曜日グループ（月水 / 火木 / 金土日）

曜日基準ではなく、実際の曜日グループを使う。日曜夜が値引ロジック上は火木基準になっても、エリア残数履歴では金土日グループに入る。

過去データは直近10件の中央値を使う。3件未満なら「過去データ不足」とし、従来の手動3択で進む。入力した残数は履歴に保存する。

快適度ポイントは履歴を分ける条件にはせず、過去中央値を補正する。プラスが大きいほど売れにくい扱い。

- 快適度 +1: 基準残数を5%下げる
- 快適度 +2: 基準残数を10%下げる
- 快適度 +3: 基準残数を15%下げる
- 快適度 -1: 基準残数を5%上げる

減り方補正も追加した。前回時刻の残数と今回残数の減少率を過去の同条件の減少率中央値と比較し、差が20ポイント以上あれば最大1段階だけ補正する。

- 過去より減りが悪い: 1段階多い側へ
- 過去より減りが良い: 1段階少ない側へ

15時→17時の減り方を見るのは、午後に追加しない前提の以下のエリアだけ。

- 弁当・麺類
- 天ぷら
- 涼味商品
- 焼鳥
- おにぎり
- いなり
- 細巻き

17時→18時30分、18時30分→19時30分は全エリアで減り方補正対象。

「迷ったら…」の文言は、曜日基準ではなく実際の曜日を見る形に変更した。

- 実際の曜日が月・水・火・木 → 多い側に寄せる
- 実際の曜日が金・土・日 → 少ない側に寄せる

主な実装ファイル:

- `src/domain/areaCountHistory.ts`
- `src/domain/discount.ts`
- `src/domain/types.ts`
- `src/hooks/useNebikiApp.ts`
- `src/components/screens/AreaJudgeScreen.tsx`
- `src/components/screens/RateDisplayScreen.tsx`

## 2026-06-24 追記：実務時刻への変更

実務上、18時30分値引は行わず、19時から夜の値引を再開する運用に合わせるため、通常値引の後半時刻表示を変更した。

- 旧 18時30分枠（内部キー `18`）を、画面上は「19時」として扱う。
- 旧 19時30分枠（内部キー `19`）を、画面上は「19時45分」として扱う。
- 通常時刻の流れは `15時 → 17時 → 19時 → 19時45分 → 20時30分最終値引`。
- 自動時刻判定は、19:00から19時枠、19:45から19時45分枠、20:30から最終値引枠。
- 完了画面の次回値引ボタンも、17時完了後は「19時の値引に進む」、19時完了後は「19時45分の値引に進む」に変更。
- 19時枠の実時間+5%は19:30以降、19時45分枠の実時間+5%は20:15以降。
- エリア残数履歴の内部 `discountTime` キーは互換性を優先して `18` / `19` を維持している。表示上の意味だけを19時 / 19時45分に変更している点に注意。

確認結果:
- `npm run check:logic` PASS
- `npm run build` PASS

## 2026-06-24 追記：正式値引時刻へ戻し、売場チェックを一旦削除

新人指導用の正式運用に合わせるため、直前の実務時刻版（19時 / 19時45分）は撤回し、値引時刻を以下へ戻した。

- 15時
- 17時
- 18時30分
- 19時30分
- 20時30分最終値引

天候入力・値引準備開始時刻は以下。

- 15時値引: 14:40
- 17時値引: 16:40
- 18時30分値引: 18:25
- 19時30分値引: 19:25
- 20時30分最終値引: 20:25

19時売場チェックは一旦導線から削除した。トップ画面には19時売場チェック入口を出さず、旧状態が残っていても通常のトップへ寄せる。

## 2026-06-24 追記：次の天候入力時刻になったら全画面から自動遷移

次の天候入力・値引準備開始時刻になった場合、完了画面に限らず、通常作業中のどの画面にいても次の値引の入力画面へ自動遷移するよう変更した。

- 15時値引中に16:40以降 → 17時の天候入力へ自動遷移
- 17時値引中に18:25以降 → 18時30分の天候入力へ自動遷移
- 18時30分値引中に19:25以降 → 19時30分の天候入力へ自動遷移
- 19時30分値引中に20:25以降 → 20時30分最終値引画面へ自動遷移

20時30分は天候入力を行わないため、20:25以降は直接 `final_time` へ進む。

主な変更ファイル:

- `src/hooks/useNebikiApp.ts`

確認:
- `npm run check:logic` PASS
- `npm run build` PASS

## 2026-06-25 追記：同じ商品カウント上限を10個固定に変更

ユーザー要望により、エリア残数入力の「同じ商品は〇個まで」の上限を、曜日基準・実際の曜日・値引時刻に関係なく常に10個固定にした。

以前のように月水・火木・金土日などで 8 / 10 / 12 個に変える運用はしない。新人に使わせる前提のため、迷わないように常に10個までで統一する。

主な変更ファイル:

- `src/domain/areaCountHistory.ts`
- `scripts/check-logic.ts`

確認:
- `npm run check:logic` PASS
- `npx tsc -b --pretty false` PASS
- Vite build PASS

## 2026-06-25 追記：ZIP返却は軽量版を基本にする

ユーザー要望により、今後ZIPを返すときは軽量版ZIPを基本にする。

軽量版ZIPでは、少なくとも以下を除外する。

- `.git/`
- `node_modules/`
- `.env`
- 秘密情報
- 作業ログ
- ZIP内に紛れ込んだ別のZIP

今回の軽量版は `dist/` を同梱した形で返している。これはビルド済みファイルも確認できるようにするため。ユーザーが「超軽量版」「distなし」と明示した場合は `dist/` も除外する。



## 2026-06-25 追記：値引開始ボタンの時刻ロック解除

ユーザー要望により、トップ画面の開始ボタンを時刻ではロックしないように変更した。

背景:
- 15時値引きの値引率表示画面を確認しようとした際、14:40前だと「15時は14:40から進めます」系の文言が出てボタンが押せなかった。
- 値引率確認用としては、開始時刻前でも進められる方が運用に合う。

変更内容:
- `src/components/screens/StartScreen.tsx` から開始時刻ゲートを削除。
- `PrimaryButton` の disabled 条件を `!allRequiredInputsConfirmed` のみに変更。
- `getSessionInputOpenMinutes` / `getSessionInputOpenText` と、時刻ゲート用の `nowMs` 更新処理を削除。
- 必要な天候入力が済んでいれば、15時・17時・18時30分・19時30分・20時30分の各値引へ時刻に関係なく進める。

残している挙動:
- 自動の値引時刻切替は残す。
- 天候入力の必須確認は残す。未確認の入力がある場合は引き続き開始できない。
- 完了画面から次の値引へ進む導線の時刻ロックは今回変更していない。今回の対象はトップ画面の開始ボタン。

検証:
- `npm run check:logic` 通過。
- `npm run build` 通過。

ZIP返却方針:
- 引き続き軽量版ZIPで返す。`.git` / `node_modules` / `.env` / 作業ログ / 余計なZIPは含めない。通常の軽量版では `dist/` は同梱可。

## 2026-06-25 追記：通常値引の値引率表示をステップ式に変更

ユーザー要望により、通常値引の値引率表示画面を、複数の値引率を一括表示する形式から、20時30分の最終値引と同じような「1指示ずつ表示して、終わったボタンで次へ進む」形式に変更した。

変更内容:

- `src/components/screens/RateDisplayScreen.tsx` を変更。
- 通常値引では、値引率カード内に `終わった` ボタンを表示する。
- `終わった` を押すたびに、同じエリア内の次の値引指示へ進む。
- 最後の値引指示で `終わった` を押すと、従来の `次のエリアへ` と同じ処理を実行する。
- 「多いのうち10個以上は 15%」などの注記がある場合は、最初に「多いのうち10個以上の商品を15%値引きしてください。」として独立した指示を出す。
- その後に「それ以外の多い商品を○%値引きしてください。」を表示する。追加目安ステップがない場合は「多い商品を○%値引きしてください。」を表示する。
- 最後に「どちらでもない商品を□%値引きしてください。」または、該当ステップでは「多くない商品を□%値引きしてください。」を表示する。
- 多い商品用の追加目安がない場合は、追加目安ステップは出さない。
- ステップ1の一律値引では「このエリアの商品を□%値引きしてください。」の1ステップだけ表示し、`終わった` で次エリアへ進む。
- 手動スキップ、スキップ先選択、注意事項表示は従来どおり残す。

確認結果:

- `npm run check:logic` PASS。主要チェックは `54 / 54 checks passed`。
- `npm run build` PASS。


## 2026-06-26 追記：値引率の絶対上限を50%に固定

ユーザー要望により、通常値引の値引率は、雨・雪補正、エリア補正、実時間補正、多い商品補正、残数目安の追加補正など、どの状況においても最高50%までに固定した。

背景:
- 値引率表示で55%が出た。
- ユーザー判断として、値引率はどんな状況でも最高50%までとする。

変更内容:
- `src/domain/discount.ts` に `MAX_DISCOUNT_RATE = 50` と絶対上限用の `capAbsoluteDiscountRate()` を追加。
- `capNormalDiscountRate()` は、時刻別上限を無視する雨雪補正中でも、最終的に50%を超えないようにした。
- 19時30分などで雨雪補正により時刻別上限を外す場合でも、50%は超えない。
- 「多いのうち10個以上は 55%」や「65%」のような追加目安も出さない。
- `scripts/check-logic.ts` の旧「雨雪補正中は上限なし」前提を更新し、雨雪補正中も絶対上限50%で止まるテストを追加した。

確認結果:
- `npm run check:logic` PASS。主要チェックは `54 / 54 checks passed`。
- `npm run build` PASS。

ZIP返却方針:
- 引き続き軽量版ZIPで返す。`.git` / `node_modules` / `.env` / 作業ログ / 余計なZIPは含めない。通常の軽量版では `dist/` は同梱可。

## 2026-06-26 追記：日付跨ぎ時の更新必須表示

ユーザー要望により、アプリを開きっぱなしにしたまま日付を跨いだ場合は、通常画面を使わせず大きな更新案内を表示するようにした。

### 仕様
- アプリ起動時のローカル日付を保持する。
- 30秒間隔、画面フォーカス復帰、タブ表示状態変更時に現在日付を確認する。
- 起動時の日付と現在日付が違う場合、通常の `AppRouter` を表示せず、更新必須画面だけを表示する。
- 表示文言は大きく「ページを更新してください」「日付が変わったため、更新しないとアプリは使えません。」とする。
- 画面内に「ページを更新する」ボタンを置き、押すと `window.location.reload()` する。
- 目的は、開きっぱなしの端末が古い残数履歴・古い当日状態のまま使われることを防ぐこと。

### 変更ファイル
- `src/app/App.tsx`

### 2026-06-26 この追記時点の確認結果
- `npm run check:logic` PASS。主要チェックは `54 / 54 checks passed`。
- `npm run build` PASS。

## 2026-06-26 追記：雨に直接1枠だけ挟まれた晴れ時刻を雨扱いに変更

ユーザー要望により、時刻別天気で「雨 → 晴れ → 雨」のように、雨に直接1枠だけ挟まれている時刻は、値引率計算上は雨として扱うようにした。

### 仕様
- 入力値そのものは書き換えない。
- 値引率計算・天候ポイント計算に使う直前で、前後の隣接時刻が両方とも雨の `sunny` だけを `rain` として扱う。
- 例: 16時が雨、17時が晴れ、18時が雨なら、17時は計算上雨扱い。
- 例: 15時が雨、16時が晴れ、17時が晴れ、18時が雨の場合、16時・17時は雨扱いしない。
- 複数時刻がまとめて雨に挟まれているだけでは雨扱いしない。あくまで「雨 → 晴れ → 雨」の1枠だけ。
- 雪は雨より重い天候として扱うため、雪を雨へ下げる変更はしない。

### 変更ファイル
- `src/domain/hourlyWeather.ts`
  - `resolveRainSandwichedHourlyForecasts()` を追加。
  - `resolveWeatherInputForDiscount()` 内で、値引率補正・未来天候ポイント・直近天気判定に反映。
- `scripts/check-logic.ts`
  - 雨に直接1枠だけ挟まれた晴れが雨扱いになること、複数の晴れが雨に挟まれても雨扱いしないこと、雪は雪のままになること、値引率補正と未来天候ポイントに反映されることを確認するテストを追加。

### 確認結果
- `npm run check:logic` PASS。
- `npm run build` PASS。

ZIP返却方針:
- 引き続き軽量版ZIPで返す。`.git` / `node_modules` / `.env` / 作業ログ / 余計なZIPは含めない。通常の軽量版では `dist/` は同梱可。

## 2026-06-26 追記：エリア判定の過去データをSupabase基準に整理

ユーザー要望により、エリア残数判定で使う過去データの扱いを整理した。

### 背景
- Supabaseから削除したはずのテスト入力が、端末内ローカル履歴に残っている可能性があった。
- その結果、同じ曜日の記録が実際より多く表示される恐れがあった。
- エリア判定は全員で共有するデータが前提なので、端末ごとに判定がズレるローカル履歴は判定材料に使わない方針にした。

### 仕様
- アプリ起動時のエリア残数履歴は、ローカル保存からは読み込まない。
- Supabaseから読み込めた履歴を、エリア判定用の履歴として扱う。
- Supabase読込時に、ローカル履歴とのマージはしない。
- エリア判定の比較サンプルは「今日より前の日付」の履歴だけを使う。
- 同じ日・同じエリア・同じ時刻の記録が複数ある場合は、`recordedAt` が最新の1件だけ使う。同時刻の場合は `sessionStartedAt` が新しい方を採用する。
- 今日入力したデータは、同じ日の過去データ件数には含めない。
- 減り方補正のための「当日の前回時刻の残数」は、同じ日内の記録として引き続き参照できるようにしている。ただし、比較サンプル自体は今日より前のデータだけ。

### 変更ファイル
- `src/hooks/useNebikiApp.ts`
  - `areaCountRecords` の初期値をローカル履歴ではなく空配列に変更。
  - Supabase読込成功時は、ローカル履歴とマージせずSupabase履歴で置き換える。
- `src/domain/areaCountHistory.ts`
  - 同日・同エリア・同時刻の重複記録を最新1件にする処理を追加。
  - エリア判定の比較サンプルから今日の記録を除外。
- `scripts/check-logic.ts`
  - 今日の記録を除外し、同日同エリア同時刻の重複は最新1件だけ使うテストを追加。

### 確認結果
- `npm run check:logic` PASS。
- `npm run build` PASS。

ZIP返却方針:
- 引き続き軽量版ZIPで返す。`.git` / `node_modules` / `.env` / 作業ログ / 余計なZIPは含めない。通常の軽量版では `dist/` は同梱可。

## 2026-06-26 追記：値引指示ステップの文字サイズを通常寄りに変更

ユーザー要望により、値引率表示ステップで「多い商品を50%値引きしてください。」などの指示文が店頭で目立ちすぎないよう、文字サイズを小さくした。

### 仕様
- ステップ式値引指示カードのメイン指示文を `fontSize: 22` / `fontWeight: 800` から `fontSize: 18` / `fontWeight: 700` に変更。
- 表示内容、ステップ順、値引率計算、50%上限などのロジックは変更していない。
- 目的は、お客さんから画面が見えたときに値引率指示が必要以上に目立つのを抑えること。

### 変更ファイル
- `src/components/screens/RateDisplayScreen.tsx`

### 確認結果
- `npm run check:logic` PASS。
- `npm run build` PASS。

ZIP返却方針:
- 引き続き軽量版ZIPで返す。`.git` / `node_modules` / `.env` / 作業ログ / 余計なZIPは含めない。通常の軽量版では `dist/` は同梱可。

## 2026-06-26 追記：値引率表示の説明文を短縮し、迷った時の曜日寄せをボタン直前へ移動

ユーザー要望により、値引率表示ステップの説明文を短くし、迷った時に「多い側」「少ない側」のどちらへ寄せるかを実際の曜日から判定して、各ステップの「終わった」ボタンの真上に表示するよう変更した。

### 仕様
- 通常値引の説明文から「完了したら以下の値引率で値引きをしてください。」の行を削除。
- 「各商品の量が『多い・少ない・どちらでもない』のどれかを確認してください。」の1文だけに整理。
- 実際の曜日が月・火・水・木の場合は、ボタン直前に「迷ったら：○曜日なので、迷ったら多い側に寄せる」と表示。
- 実際の曜日が金・土・日の場合は、ボタン直前に「迷ったら：○曜日なので、迷ったら少ない側に寄せる」と表示。
- 画面下部の「迷ったら…」欄から曜日別の寄せ方説明は削除し、アウトパック・大/小パック・期限違いの補足だけを残した。
- 値引率計算、ステップ順、50%上限、エリア判定ロジックは変更していない。

### 変更ファイル
- `src/components/screens/RateDisplayScreen.tsx`
  - `getWeekdayBiasNotice()` を追加。
  - `RateInstructionCard` に `weekdayBiasNotice` を渡し、「終わった」ボタンの直前へ表示。
  - 通常値引の説明文を短縮。
  - 画面下部の曜日別「迷ったら」説明を削除。

### 確認結果
- `npm run check:logic` PASS。
- `npm run build` PASS。

ZIP返却方針:
- 引き続き軽量版ZIPで返す。`.git` / `node_modules` / `.env` / 作業ログ / 余計なZIPは含めない。通常の軽量版では `dist/` は同梱可。

## 2026-06-26 追記：時刻手動選択UIの誤変更を戻し、天候入力中の時刻ズレを修正

ユーザー指摘により、前回の「時刻を常にプルダウン表示にする」変更は意図と違っていたため採用しない。開始画面の時刻欄は元の仕様に戻し、自動中は表示のみ、手動切替時だけプルダウンを出す。

### 背景
- 19時30分の天候入力をしていたのに、実時間が20時台へ進むと20時30分の値引率表示画面が出ることがあった。
- 原因は、開始画面で実時間による自動時刻同期が動き続け、天候入力中の `sessionDraft.discountTime` が次の値引帯へ進んでしまう可能性があったこと。
- また `startSession()` が、手動時刻指定でない場合に現在時刻ベースの `resolveDiscountTime(now)` を優先していたため、入力した時刻ではなく現在時刻の値引帯で開始される可能性があった。

### 仕様
- 時刻UIは元の仕様に戻す。
  - 自動中：現在の値引時刻を表示し、右ボタンは「手動で切り替える」。
  - 手動中：プルダウンを表示し、右ボタンは「自動に戻す」。
- 天候入力を変更した時点で、その時点の値引時刻を `weatherInputLockedDiscountTime` として一時ロックする。
- 一時ロック中は、開始画面を開いたまま実時間が次の値引帯へ進んでも、値引時刻を自動で20時30分などへ進めない。
- 「手動で切り替える」や「自動に戻す」を押した場合は、一時ロックを解除する。
- `startSession()` は、手動指定があれば手動時刻、天候入力ロックがあればロック時刻、どちらもなければ現在時刻を使う。
- 天候入力ロックは手動時刻指定とは別扱い。`manualDiscountTimeOverride` は立てないため、実時間による遅れ補正（例：19時30分基準で20時15分以降 +5%）は従来どおり使える。
- 日付が変わった場合は既存の日付跨ぎ更新必須画面で止めるため、前日の天候入力ロックを翌日に持ち越さない。

### 変更ファイル
- `src/domain/types.ts`
  - `SessionDraft.weatherInputLockedDiscountTime?: DiscountTime | null` を追加。
- `src/hooks/useNebikiApp.ts`
  - `isValidDiscountTime()` を追加。
  - `normalizeSessionDraft()` で天候入力ロックを復元。
  - `buildStartDefaultDraft()` で同日分の天候入力ロックだけ維持し、別日分は破棄。
  - 開始画面の自動時刻同期で、天候入力ロック中は `discountTime` を更新しないよう変更。
  - `updateSessionDraft()` で天候入力変更時に現在の `discountTime` を一時ロック。
  - `startSession()` で「手動時刻 > 天候入力ロック時刻 > 現在時刻」の優先順に変更。

### 確認結果
- `npm run check:logic` PASS。
- `npm run build` PASS。

ZIP返却方針:
- 引き続き軽量版ZIPで返す。`.git` / `node_modules` / `.env` / 作業ログ / 余計なZIPは含めない。通常の軽量版では `dist/` は同梱可。

## 2026-06-26 追記：天候入力中の時刻ズレ修正を再調整

ユーザーから「19時半で天候入力しても20時半の値引率表示画面が出る問題が直っていない」と指摘があったため、前回修正をさらに見直した。

### 背景
- 前回は `weatherInputLockedDiscountTime` を追加したが、それでも開始時に現在時刻で再解決される経路や、開始画面表示中に自動次セッション遷移が走る経路が残っていた。
- その結果、画面上では19時30分の天候入力をしているのに、開始ボタン押下時または開始画面表示中の自動遷移で20時30分扱いになる可能性があった。

### 今回の仕様
- `startSession()` では、クリック時点の現在時刻で `resolveDiscountTime(now)` を再実行して値引時刻を決めない。
- 開始時は、画面に表示されている `sessionDraft.discountTime` をそのまま `session.discountTime` として使う。
- これにより、19時30分表示の状態で天候入力して開始した場合は、実時間が20時30分帯に入っていても19時30分の値引画面へ進む。
- 作業中・完了画面では従来どおり次の時刻が来たら自動遷移できる。
- ただし `state.screen === "start"` の開始画面では、自動次セッション遷移を走らせない。
- これは、開始画面で天候入力中に20時30分へ勝手に飛ぶのを防ぐため。

### 変更ファイル
- `src/hooks/useNebikiApp.ts`
  - `startSession()` の値引時刻決定を `prev.sessionDraft.discountTime` 固定に変更。
  - 次セッション自動遷移 `useEffect` に `state.screen === "start"` のガードを追加。

### 注意
- UI表示と開始後の値引時刻を一致させることを優先する。
- 自動時刻同期は開始画面で引き続き動くが、開始ボタン押下時に現在時刻で裏から別時刻へ差し替えることはしない。

### 確認結果
- `npm run check:logic` PASS。
- `npm run build` PASS。

ZIP返却方針:
- 引き続き軽量版ZIPで返す。`.git` / `node_modules` / `.env` / 作業ログ / 余計なZIPは含めない。通常の軽量版では `dist/` は同梱可。

## 2026-06-26 追記：19時30分入力中に20時30分へ飛ぶ問題の追加修正

ユーザーから前回修正後も「飛んじゃう」と指摘があったため、天候入力ロックの競合をさらに潰した。

### 原因として残っていた可能性
- `updateSessionDraft()` 側で天候入力時のロック時刻を `prev.sessionDraft.discountTime` から取っていた。
- そのため、時刻境界付近で開始画面の自動時刻同期が先に走ると、画面上では19時30分を見て入力していても、親state側の `prev.sessionDraft.discountTime` が20時30分に更新済みになり、ロック自体が20時30分になる競合があり得た。
- また開始時も、ロック時刻より `sessionDraft.discountTime` を優先する経路が残っていた。

### 今回の仕様
- 天候入力画面で天候・気温・風のどれかを入力/確定した時、StartScreenがその時点で表示している `sessionDraft.discountTime` を `weatherInputLockedDiscountTime` として明示的に渡す。
- `updateSessionDraft()` は、天候入力パッチに明示ロック時刻が含まれる場合、それを最優先で使う。
- 明示ロックがない場合だけ、既存ロックまたは現在のdraft時刻を使う。
- `startSession()` は、手動時刻指定がない場合、`weatherInputLockedDiscountTime` が有効ならそれを `session.discountTime` として使う。
- これにより、19時30分表示中に天候入力を始めた後、実時間が20時25分以降になっても、開始後は19時30分の値引率表示へ進む。
- 手動時刻指定時は従来どおり手動指定を優先する。

### 変更ファイル
- `src/components/screens/StartScreen.tsx`
  - `applyHourlyChange()` で、手動時刻指定でない通常値引の天候入力時に `weatherInputLockedDiscountTime: sessionDraft.discountTime` を明示的に渡す。
- `src/hooks/useNebikiApp.ts`
  - `updateSessionDraft()` で明示ロック時刻を優先。
  - `startSession()` で `manualDiscountTimeOverride` がなく、有効な `weatherInputLockedDiscountTime` がある場合はその時刻で開始。

### 確認結果
- `npm run check:logic` PASS。
- `npm run build` PASS。

ZIP返却方針:
- 引き続き軽量版ZIPで返す。`.git` / `node_modules` / `.env` / 作業ログ / 余計なZIPは含めない。通常の軽量版では `dist/` は同梱可。

## 2026-06-26 追記：URLパラメータ式の動作確認モードを追加

ユーザーの目的は本番運用ではなく、各値引時刻の動作確認。実時間による自動判定と競合して19時30分確認中に20時30分へ飛ぶ問題が続いたため、本番UIの手動時刻選択をいじるのではなく、URLパラメータで実時間を固定できる動作確認モードを追加した。

### 仕様
- 通常URLでは従来どおり実時間で動く。
- `?testTime=1930` のように付けた時だけ、アプリ内の現在時刻を固定する。
- 対応例:
  - `?testTime=1500` または `?testTime=15` → 15時固定
  - `?testTime=1700` または `?testTime=17` → 17時固定
  - `?testTime=1830` または `?testTime=18` → 18時30分固定
  - `?testTime=1930` または `?testTime=19` → 19時30分固定
  - `?testTime=2030` または `?testTime=20` → 20時30分固定
- `?testDate=YYYY-MM-DD` を併用すると、曜日判定用の日付も固定できる。
  - 例: `?testTime=1930&testDate=2026-06-24`
- 動作確認モード中は左上に小さく「動作確認モード：YYYY-MM-DD ○時固定」と表示する。
- 動作確認モード中は日付跨ぎ更新ブロッカーを出さない。固定したテスト日付・時刻で動作確認できるようにするため。

### 実装方針
- `src/app/App.tsx` で URLSearchParams から `testTime` / `testDate` を読む。
- 解析した固定日時を `useNebikiApp({ testNow })` に渡す。
- `src/hooks/useNebikiApp.ts` に runtime now override を追加し、アプリ内の `new Date()` / `Date.now()` 参照をテスト時刻に差し替えられるようにした。
- `src/app/AppRouter.tsx` 経由で `StartScreen` にも `testNow` を渡し、開始画面の「自動に戻す」操作でもテスト時刻を使うようにした。
- 本番URLでは override は `null` になり、通常どおり実時間を使う。

### 変更ファイル
- `src/app/App.tsx`
  - `getCurrentTestMode()` / `parseTestTime()` / `parseTestDate()` を追加。
  - 動作確認モード表示バナーを追加。
  - `useNebikiApp` に `testNow` を渡す。
- `src/app/AppRouter.tsx`
  - `testNow` props を追加し、`StartScreen` へ渡す。
- `src/components/screens/StartScreen.tsx`
  - `now?: Date` props を追加。
  - 曜日・時刻を「自動に戻す」時に、実時間ではなく渡された `now` を使う。
- `src/hooks/useNebikiApp.ts`
  - `runtimeNowOverrideMs` / `getRuntimeNow()` / `getRuntimeNowMs()` を追加。
  - `useNebikiApp` の引数に `testNow?: Date | null` を追加。
  - 初期セッション、日付、現在時刻更新、開始処理、リセット処理などの現在時刻取得で override を使うように変更。

### 確認結果
- `npm run check:logic` PASS。
- `npm run build` PASS。

ZIP返却方針:
- 引き続き軽量版ZIPで返す。`.git` / `node_modules` / `.env` / 作業ログ / 余計なZIPは含めない。通常の軽量版では `dist/` は同梱可。

## 2026-06-26 追記：動作確認モードでは残数入力を保存しない

ユーザーから「動作確認モードでは残数入力してもSupabaseにもローカルにも保存されないようにできる？」と依頼があったため、テスト用URLパラメータ利用中は入力結果を永続保存しないようにした。

### 仕様
- `?testTime=1930` などの動作確認モード中は、残数入力をしてもエリア残数履歴を保存しない。
- Supabaseへ `area_count_records` の upsert をしない。
- 端末内 `localStorage` へアプリ状態・画面遷移状態を保存しない。
- これにより、テスト中に入力した残数が本番判定用データや通常利用時の再開状態を汚さない。
- Supabaseからの既存履歴読込は維持する。判定表示の動作確認では、本番と同じ過去履歴を参照できるようにするため。
- 通常URLでは従来どおり保存する。

### 実装
- `src/hooks/useNebikiApp.ts`
  - `const isTestMode = params?.testNow instanceof Date;` を追加。
  - 永続化用 `useEffect` で、動作確認モード中は `savePersistedNebikiState()` / `saveWorkSessionCheckpoint()` を実行しない。
  - runtime state保存用 `useEffect` で、動作確認モード中は `saveRuntimeState()` を実行しない。
  - `judgeCurrentArea()` のエリア残数履歴保存条件に `!isTestMode` を追加し、`setAreaCountRecords()` と `upsertRemoteAreaCountRecord()` をスキップ。
  - `migrateLocalAreaCountRecordsToRemote()` も動作確認モード中は実行しない。

### 確認結果
- `npm run check:logic` PASS。
- `npm run build` PASS。

ZIP返却方針:
- 引き続き軽量版ZIPで返す。`.git` / `node_modules` / `.env` / 作業ログ / 余計なZIPは含めない。通常の軽量版では `dist/` は同梱可。

## 2026-06-26 追記：値引率表示の「迷ったら」を曜日基準から時刻基準へ変更

ユーザー要望により、値引率表示ステップの「迷ったら」文言を、実際の曜日ではなく値引時刻の方針で分ける形に変更した。

### 仕様
- 15時は品揃え確保優先として扱う。
  - 表示文言: `迷ったら：15時は品揃え確保優先なので、迷ったら少ない側に寄せる`
- 17時以降（17時・18時30分・19時30分）は売り切り優先として扱う。
  - 表示文言: `迷ったら：17時以降は売り切り優先なので、迷ったら多い側に寄せる`
- 20時30分は最終値引画面のため、通常値引ステップ側の「迷ったら」表示は使わない。
- 表示位置は従来通り、各値引指示ステップの「終わった」ボタン直前。
- 値引率計算・ステップ順・エリア判定ロジックは変更していない。

### 実装
- `src/components/screens/RateDisplayScreen.tsx`
  - `getWeekdayBiasNotice(weekdayText)` を廃止。
  - `getTimeBiasNotice(discountTime)` を追加。
  - `RateInstructionCard` の props を `weekdayBiasNotice` から `timeBiasNotice` に変更。
  - `discountTime` を基準に「少ない側」「多い側」の文言を出すよう変更。

### 確認結果
- `npm run check:logic` PASS。
- `npm run build` PASS。

ZIP返却方針:
- 引き続き軽量版ZIPで返す。`.git` / `node_modules` / `.env` / 作業ログ / 余計なZIPは含めない。通常の軽量版では `dist/` は同梱可。

## 2026-06-26 追記：値引率表示の「迷ったら」をボタン直前から画面下部欄へ戻す

ユーザー要望により、値引率表示ステップの「迷ったら」文言を、各ステップの「終わった」ボタン直前に表示する形から、以前の画面下部の「迷ったら…」欄に戻した。

### 仕様
- 「多い側」「少ない側」という表現は維持する。
  - これは「多い・少ない・どちらでもない」を判断するときに、迷ったらどちらへ分類を寄せるかを示すため。
- 表示位置は、各ステップの「終わった」ボタン直前ではなく、画面下部の「迷ったら…」欄。
- 「迷ったら…」欄には従来の補足に加え、時刻基準の寄せ方を表示する。
  - 15時: `➡少ない側に寄せる`（品揃え確保優先）
  - 17時以降: `➡多い側に寄せる`（売り切り優先）
- 値引率計算・ステップ順・エリア判定ロジックは変更していない。

### 実装
- `src/components/screens/RateDisplayScreen.tsx`
  - `getTimeBiasNotice()` と `TimeBiasNotice` を削除。
  - `RateInstructionCard` から `timeBiasNotice` props とボタン直前の表示ブロックを削除。
  - 画面下部セクションの見出しを `補足` から `迷ったら…` に戻し、時刻基準の寄せ方を追記。

### 確認結果
- `npm run check:logic` PASS。
- `npm run build` PASS。

ZIP返却方針:
- 引き続き軽量版ZIPで返す。`.git` / `node_modules` / `.env` / 作業ログ / 余計なZIPは含めない。通常の軽量版では `dist/` は同梱可。

## 2026-06-26 追記：値引率表示の「迷ったら…」をタップ式ポップアップに変更

ユーザー要望により、値引率表示画面の「迷ったら…」を常時表示の画面下部欄ではなく、各値引指示ステップの「終わった」ボタン直前に置いたタップ式リンクに変更した。

### 仕様
- 通常値引のステップ表示中、「終わった」ボタンの上に `迷ったら…` を表示する。
- `迷ったら…` をタップすると、迷った時の判断基準をポップアップで表示する。
- ポップアップ内の `OK` を押すと閉じる。
- 背景をタップしても閉じる。
- 画面下部に常時表示していた「迷ったら…」欄は削除した。
- 判断基準の内容は前回の時刻基準を維持する。
  - アウトパック: 多い側に寄せる。
  - 大パック・小パックに分かれている場合: 大パックだけ値引。
  - 期限が近いものと遠いもので分かれている場合: 近いものだけ値引。
  - 分かれていなければ、15時は少ない側に寄せる（品揃え確保優先）。
  - 分かれていなければ、17時以降は多い側に寄せる（売り切り優先）。
- 値引率計算・ステップ順・保存処理・エリア判定ロジックは変更していない。

### 実装
- `src/components/screens/RateDisplayScreen.tsx`
  - `JudgeHintContent` を追加。
  - `JudgeHintDialog` を追加。
  - `RateInstructionCard` 内に `showJudgeHint` state を追加。
  - `終わった` ボタン直前に、透明背景の `迷ったら…` ボタンを追加。
  - 既存の画面下部「迷ったら…」常時表示セクションを削除。

### 確認結果
- `npm run check:logic` PASS。
- `npm run build` PASS。

ZIP返却方針:
- 引き続き軽量版ZIPで返す。`.git` / `node_modules` / `.env` / 作業ログ / 余計なZIPは含めない。通常の軽量版では `dist/` は同梱可。

## 2026-06-26 追記：値引率表示の「迷ったら…」リンク位置調整

ユーザー要望により、値引率ステップ表示の `迷ったら…` が `終わった` ボタンに近すぎたため、進行表示（例: `1 / 3`）の右側に移動した。

### 仕様
- 値引指示ステップ上部に、左側 `1 / 3`、右側 `迷ったら…` の横並び行を表示する。
- `迷ったら…` をタップすると従来どおり判断基準ポップアップを表示する。
- `終わった` ボタン直前の `迷ったら…` は削除する。
- `終わった` ボタンとの距離を確保するため、値引指示本文の下にボタンだけを配置する。
- 判断基準の内容、値引率計算、ステップ順、保存処理、エリア判定ロジックは変更していない。

### 実装
- `src/components/screens/RateDisplayScreen.tsx`
  - `RateInstructionCard` の上部に flex 行を追加。
  - `totalCount > 1` のときは左に進行表示、右に `迷ったら…` ボタンを表示。
  - `totalCount === 1` の場合も右側に `迷ったら…` を表示できるように `justifyContent` を調整。
  - `終わった` ボタン上の旧リンクブロックを削除。

### 確認結果
- `npm run check:logic` PASS。
- `npm run build` PASS。

ZIP返却方針:
- 引き続き軽量版ZIPで返す。`.git` / `node_modules` / `.env` / 作業ログ / 余計なZIPは含めない。通常の軽量版では `dist/` は同梱可。

## 2026-06-27 追記：エリア残数入力を画面内キーパッド方式に変更

ユーザー要望により、エリア残数入力でスマホのキーボードが出ないよう、通常の数値入力欄を廃止し、画面内の数値ボタンで入力する方式に変更した。

### 仕様
- 残数入力の説明に「消費期限が今日までの商品」を明記する。
  - 見出し: `このエリア全体で、消費期限が今日までの商品数は？`
  - ラベル: `消費期限が今日までの商品数`
- 入力部分は `<input>` ではなく表示用の領域にする。
- 画面内に `0〜9` の数字ボタンとバックスペースボタン `⌫` を表示する。
- 数字ボタンを押すと桁が追加される。
- 先頭の余分な 0 は残さない。
- `⌫` を押すと1文字削除する。
- 未入力時は `未入力` と表示する。
- 0個を入力したい場合は `0` ボタンを押す。
- 残数判定ロジック、Supabase保存方針、動作確認モード中の非保存仕様は変更していない。

### 実装
- `src/components/screens/AreaJudgeScreen.tsx`
  - `handleAreaCountDigit()` を追加。
  - `handleAreaCountBackspace()` を追加。
  - 既存の `<input type="number">` を削除。
  - 表示領域 + 画面内キーパッドに置き換え。

### 確認結果
- `npm run check:logic` PASS。
- `npm run build` PASS。

ZIP返却方針:
- 引き続き軽量版ZIPで返す。`.git` / `node_modules` / `.env` / 作業ログ / 余計なZIPは含めない。通常の軽量版では `dist/` は同梱可。

## 2026-06-27 追記：残数入力キーパッドの完了ボタン配置と手動判定フォーカス

ユーザー要望により、エリア残数入力の画面内キーパッド下段を、左から `0` / `⌫` / `完了` に変更した。あわせて、`完了` を押したときに過去データ不足で自動判定できない場合は、手動5段階判定の `普通` ボタンへフォーカスし、選びやすい状態にした。

### 仕様
- 数値キーパッドは3列のままにする。
- 下段は左から `0`、`⌫`、`完了`。
- 残数が未入力の場合、`完了` は押せない。
- 残数入力後に `完了` を押した場合:
  - 過去データが足りて自動エリア残数判定が使えるなら、その判定で進む。
  - 過去データが足りない場合は、手動5段階判定の `普通（±0%）` ボタンへスクロール・フォーカスする。
  - このとき `普通` ボタンを青枠で強調する。
- 数字やバックスペースを押して入力値を変えた場合、`普通` ボタンの強調は解除する。
- 値引率計算、Supabase保存方針、動作確認モード中の非保存仕様は変更していない。

### 実装
- `src/components/screens/AreaJudgeScreen.tsx`
  - `JudgeOptionButton` に `buttonRef` を渡せるようにした。
  - `normalManualJudgeButtonRef` を追加。
  - `highlightNormalManualJudge` state を追加。
  - `handleAreaCountComplete()` を追加。
  - `完了` ボタンの disabled 条件を `parsedAreaCount === null` に変更。
  - `isAreaCountReady` なら従来どおり推薦判定で進む。
  - `!isAreaCountReady` なら `普通` ボタンへ `scrollIntoView()` + `focus()` し、青枠で強調する。

### 確認結果
- `npm run check:logic` PASS。
- `npm run build` PASS。

ZIP返却方針:
- 引き続き軽量版ZIPで返す。`.git` / `node_modules` / `.env` / 作業ログ / 余計なZIPは含めない。通常の軽量版では `dist/` は同梱可。
