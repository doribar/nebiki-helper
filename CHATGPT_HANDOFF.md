# ChatGPT 引き継ぎメモ — 値引ヘルパー

最終更新: 2026-07-14（日本時間）

このファイルは、値引ヘルパー本体ZIPに同梱する引き継ぎ用メモです。次のChatGPTセッションでは、ユーザーがZIPを渡したら、最初にこのファイルを読んでから作業してください。

## 0. このメモの管理ルール

### 正本
- ユーザーがそのセッションで渡した最新ZIPの中身を正本とする。
- 同一セッションで新しいZIPを複数作った場合は、最後に作ったZIPを最新の正本とする。
- ZIP名だけで新旧を推測せず、中身と更新時刻を確認する。
- 本メモとコードが食い違う場合は、実際のコードを確認し、ユーザーに勝手な推測を返さない。

### メモリ更新とファイル更新を混同しない
- ChatGPTのメモリ機能への保存は、ZIP内のファイルを変更しない。
- ユーザーが「引き継ぎメモを更新」と言った場合は、必ずZIP内ルートの `CHATGPT_HANDOFF.md` を実際に編集する。
- 編集後はファイルを再度開いて内容と行数を確認し、その編集済みファイルを含む新しいZIPを作る。
- ZIP作成後は一度展開または一覧確認し、`CHATGPT_HANDOFF.md` が更新版になっていることを確認する。
- 実ファイルを編集していないのに「引き継ぎメモを更新済み」と答えてはいけない。

### 長文化防止
- このメモは **1000行以内** を絶対上限にする。目標は700行以内。
- 小さな修正を時系列で無制限に追記しない。
- 古い変更履歴は、現在も必要な仕様だけ「現行仕様」へ統合し、重複・廃止・上書き済みの履歴は削除する。
- 900行を超えたら、次のZIP返却前に必ず整理・統合する。
- 過去の細かい経緯より、現在の正しい挙動・重要な理由・未解決点を優先する。
- この2026-07-11版では、約2573行まで膨らんだ旧メモを現行仕様中心に再構成した。

## 1. 作業開始時チェックリスト

1. ZIPを展開し、ルートの `CHATGPT_HANDOFF.md` を読む。
2. `package.json`、主要ソース、ユーザーが指定した画面・機能を確認する。
3. 変更が大きい、複数ファイルにまたがる、または検証が複雑な場合は、必要に応じてエージェント機能の利用をユーザーへ提案する。小さな文言・CSS修正は通常チャットでよい。
4. ユーザーが求めていない仕様を勝手に追加・変更しない。
5. 変更後は `npm run check:logic`、TypeScript、`npm run build` を確認する。
6. ZIPを返す直前に、このメモを現状へ更新する。

## 2. ZIP作成・命名ルール

### ファイル名
- 修正版ZIP名は日本時間の日付と時刻を入れる。
- 形式: `nebiki-helper-YYYYMMDD-HHMM.zip`
- `fixed` は入れない。
- 例: `nebiki-helper-20260711-1156.zip`

### ZIP内容
- ZIP内のトップフォルダ名は `nebiki-helper`。
- 原則として軽量版にする。
- 含めないもの:
  - `.git/`
  - `node_modules/`
  - `dist/`
  - `.env`
  - APIキー・秘密情報
  - 実写真
  - 一時ログ
  - 作業用バックアップ
  - 別のZIP
- `package.json` と `package-lock.json` は含める。
- ユーザーの作業フォルダにある `.git` はZIPに入らない。フォルダを丸ごと置き換えると `.git` が消えるため注意する。

## 3. ユーザー環境・起動

主な作業場所:

```text
C:\Users\s0a6g\Desktop\nebiki-helper
```

PowerShell:

```powershell
cd C:\Users\s0a6g\Desktop\nebiki-helper
npm install
npm run check:logic
npm run build
npm run dev
```

デプロイ先:

```text
https://nebiki-helper.vercel.app/
```

新人用step1:

```text
https://nebiki-helper.vercel.app/#/step1
```

### PWA独立アプリのステップ設定

- トップ画面右上の歯車ボタンから管理者設定を開く。
- 初回は4〜8桁の数字で設定用PINを作成する。以後は歯車を押すたびにPIN入力が必要。
- 設定画面でstep1〜step8を選び、「このステップに変更する」で端末へ保存する。
- 保存先は同一オリジンのlocalStorage。PWAのアイコンからハッシュなしで起動した場合も、保存したstepを使う。
- `#/step1` など明示的なURLを開いた場合は、そのURLのstepを優先する。設定画面からstepを保存すると明示ハッシュを外し、保存stepへ切り替える。
- PINは端末内にSHA-256ハッシュで保存する。Supabaseには送らない。アプリデータやブラウザデータを消すとPINと保存stepも初期化されるため、本格的な端末管理用認証ではなく、現場で新人が設定を変えないための管理者ロックとして扱う。

動作確認URLはクエリをハッシュより前に置く。

```text
https://nebiki-helper.vercel.app/?testTime=1930#/step1
https://nebiki-helper.vercel.app/?testTime=1930&testDate=2026-07-11#/step8
```

主な `testTime`:

- `1500` / `15`
- `1700` / `17`
- `1830` / `18`
- `1930` / `19`
- `2030` / `20`

動作確認モードでは本番のlocalStorage状態を読み込まず、本番データを保存しない。

## 4. 技術構成

- React 19
- TypeScript 5.9
- Vite 8
- 主な状態管理: `src/hooks/useNebikiApp.ts`
- 画面ルーティング: `src/app/AppRouter.tsx`
- 値引率: `src/domain/discount.ts`
- 天候・基本値引率: `src/domain/weekdayBase.ts`, `src/domain/hourlyWeather.ts`
- 祝日: `src/domain/japaneseHoliday.ts`
- エリア残数履歴: `src/domain/areaCountHistory.ts`, `src/domain/areaCountRemoteStorage.ts`
- 19:00チェック: `src/domain/review19.ts`
- 保存・日付整合性: `src/domain/storage.ts`
- 新人ステップ: `src/domain/trainingMode.ts`
- スワイプ: `src/hooks/useSwipeToSkip.ts`
- PWA生成: `vite-plugin-pwa`、設定は `vite.config.ts`

### PWA（2026-07-13追加）
- Galaxyへ「値引ヘルパー」という独立アプリとしてインストールできる。
- Web App ManifestとService Workerは `vite-plugin-pwa` がビルド時に生成する。
- 表示形式は `standalone`、縦向き、アプリ名は「値引ヘルパー」。
- アイコンは `public/pwa-192x192.png`、`public/pwa-512x512.png`、`public/pwa-maskable-512x512.png`。
- `registerType: "autoUpdate"`、`skipWaiting`、`clientsClaim` を使用し、更新版へ切り替わりやすくしている。
- アプリ本体の静的ファイルはキャッシュされるため、一度読み込んだ後はオフラインでも画面を起動できる。
- Supabaseへの保存は従来どおり通信が必要。オフライン時の送信待ち・自動再送は未実装。
- PWA化はブラウザUIを消して独立アプリ化するものであり、端末の他アプリ利用を封じる機能ではない。Galaxy側の「アプリ固定」と解除PINを併用する。
- Chromeでデプロイ済みURLを開き、「アプリをインストール」または「ホーム画面に追加」から導入する。
- Service Workerの確認は `npm run build` 後、`dist/manifest.webmanifest`、`dist/sw.js`、`dist/registerSW.js` の生成を確認する。

AI写真判定は廃止済み。写真撮影画面・写真判定サーバー・写真判定コードを復活させない。

## 5. 現在の値引率の基本仕様

### 通常時刻の基本値引率
- 15時: 0%
- 17時: 10%
- 18時30分: 20%
- 19時30分: 30%

曜日で基本値引率は変えない。曜日差はエリア残数判定側で扱う。

### 上下限
- 通常値引は時刻別上限を設けない。
- 最終的な表示は0%〜50%に丸める。
- 絶対上限は50%。

### エリア残数の5段階補正
- 多い: +10%
- やや多い: +5%
- 普通: 0%
- やや少ない: -5%
- 少ない: -10%

### 個別商品
- 多い商品: エリア表示値引率より+10%。
- 多い商品のうち10個以上: さらに+5%、つまりエリア表示値引率より+15%。
- 10個以上の基準は曜日・時刻にかかわらず固定。
- 値引率表示の指示文は、現在次の形。

```text
多い商品を30%値引きしてください。
そのうち10個以上ある商品は35%で値引きしてください。
```

数字は計算結果で変わる。

### 最終値引
- 20時30分にも天候入力を行う。入力対象は21時の天気・気温・風速。
- 入力した21時条件からA/B/Cの3段階基準を自動選択する。
- 最低基準Aは1個30%、2個40%、3個以上50%。

## 6. 新人向けステップ

残数入力はstep1〜step8で共通。違うのは、個別商品判断をどこまで表示するか。

- step1: エリアの表示値引率で一律値引。
- step2: 曜日・時刻を基準に多い商品を判断し、多い商品だけ+10%。
- step3: 商品を「多い・少ない・どちらでもない」に分け、画面に表示された値引指示を行う。
- step4: 多い商品のうち10個以上ある商品だけ、さらに+5%して合計+15%。
- step5: 定番商品・夜によく売れる商品を-10%。粗利損を防ぐ判断を先に解禁する。
- step6: 見た目が悪い商品・不人気商品を+10%。
- step7: 現在の残数だけでなく、前回の値引時刻からの商品の減り方も判断に含める。
- step8: 広告商品の当日の売れ方まで含める全解禁。

重要:
- 「曜日・時刻を基準に考えて」はstep2から表示する。多い・少ないを絶対数だけで覚えさせない。
- step3以降でも「少ない商品」を独立した値引指示カードや完了サマリーの作業項目として表示しない。「少ない判定の商品は最終値引以外では引かない」という継続ルールに見える注意文も表示しない。各値引時刻で改めて判断し、その回に少ない商品は結果として指示対象に出さない。
- step4以降は「10個以上あるだけで多いになるわけではない」と明示する。先に曜日・時刻を基準に多いと判断し、そのうち10個以上だけを追加で+5%する。
- 表示順は「多い商品、そのうち10個以上」→「どちらでもない商品」。
- step1では手動5段階判定を新人へ要求しない。履歴不足時は通常評価を使う。
- URLは `#/step1` から `#/step8`。ハッシュなしURLは完成版のstep8として扱う。

## 7. エリア残数入力・履歴判定

### 入力
- 通常値引の15時・17時・18時30分・19時30分で、エリア全体の当日期限商品数を入力する。
- 同一商品は最大10個として数える。
- 足し算用の電卓を通常残数入力画面に用意している。
- 残数入力画面は、足し算電卓が開いた状態をデフォルトにする。通常値引・20時30分最終値引・19:00チェックで共通。
- 電卓は数字、`+`、1文字削除、完了。クリアボタンは表示しない。
- `完了`を押すと合計を残数欄へ自動入力し、電卓を閉じる。必要なら画面上の`電卓`ボタンから再度開ける。
- 電卓内に「足し算電卓」という見出しは表示しない。ボタン名は「電卓」。
- 手動5段階判定が必要な画面には `迷ったら…` を表示し、値引指示画面と同じ判断基準ダイアログを開く。
- 電卓へ途中まで式を入力した状態でスキップした場合、同じセッション・同じエリアの下書きを `sessionStorage` に保持する。後で戻ると電卓が開いた状態で式が復元される。
- 電卓の`完了`で合計を残数欄へ移した時、またはエリア判定を完了した時は、そのエリアの電卓下書きを削除する。

### 履歴の正本
- エリア残数履歴はSupabaseを正とする。
- 端末内の旧ローカル履歴は判定に混ぜない。
- Supabase設定がない、または取得できない場合は履歴0件として手動判定へ進む。
- 新しい入力は画面中のstateへ反映し、Supabaseへ送る。通信失敗時に旧ローカル履歴へ退避しない。

### 自動判定
- 同じエリア・同じ時刻・同じ実際の曜日の過去データが3件以上なら、その曜日を優先。
- 祝日などで暫定グループ強制条件がある場合は同曜日より暫定グループを使う。
- 同曜日が3件未満なら暫定グループ（月水 / 火木 / 金土日）へフォールバック。
- 短期16件、長期52件を使う。
- 最近の中央値が長期より低い場合、長期中央値-2個より基準を下げないガードがある。
- 天候で過去残数中央値を補正しない。天候は値引率補正側だけで扱う。

### 暫定曜日グループ
- 祝日前日: 金土日固定。
- 金曜祝日: 金土日固定。
- 土曜祝日: 土曜の同曜日データが3件以上なら土曜を優先。足りなければ金土日。
- 日曜・祝日で翌日も休日: 金土日。
- 日曜・祝日で翌日が平日: 15時は金土日、17時以降は火木。
- その他は実際の曜日を優先し、足りなければ通常グループ。

### 手動5段階判定の保存
履歴不足時にユーザーが選んだ判定は正確に保存する。

```json
{
  "userJudge": "slightly_many",
  "suggestedEvaluation": "slightly_many",
  "areaRateAdjustment": 5
}
```

作業中のエリア進捗には判定元を保存する。

```json
{
  "areaCountEvaluation": "slightly_many",
  "areaCountEvaluationSource": "manual"
}
```

履歴自動判定なら `areaCountEvaluationSource: "history"`。

## 8. 通常値引のスキップ・後回し

- 手動スキップと少ない判定は後回し対象。
- 後回し巡回は理由グループより、現在位置からの近さを優先。
- 同じエリアを再度スキップすると、そのエリアを一時的に後ろへ回す。
- 後で戻って値引完了したエリアは `status: completed` にし、`skipReason` を削除する。
- 完了済みなのに `skipReason: "manual"` が残らないこと。

## 9. 19:00残数チェック

### 目的
17時値引後、19:00時点で当日期限商品が何個残っているかをエリア別に記録する。

### 開始
- 18時30分の天候入力画面下部に「19:00チェックを始める」を表示する。
- 現在時刻19:00以降という制限はない。
- 19:00チェック中は通常値引の自動時刻遷移を走らせない。

### 画面
- 1エリアずつ残数入力。
- エリア名と「電卓」ボタンは同じ行。
- エリア名の下に不要な空白行や「19:00時点の残数は？」を置かない。
- 上部説明は「17時値引き後、19:00時点で消費期限が今日までの商品が何個残っているかを記録します。」のみ。
- 通常残数入力と同じ足し算電卓を使える。閉じると合計を入力欄へ反映。
- 電卓へ途中まで式を入力してスキップした場合も、後で同じエリアへ戻ると電卓の式を復元する。

### スキップ
- ボタン文言は通常値引画面と同じ `今はスキップ（画面左スワイプ）`。
- 19:00チェックのスキップは対象外登録ではなく、現在エリアを末尾へ回すだけ。
- 左スワイプも同じ動作。
- 後で必ず戻る。
- 全対象エリアの残数が入るまで保存完了しない。

### 保存データ
主な構造:

- `Review19Result.areaCounts`: 19:00残数。
- `snapshot`: 17時値引セッションの判断内容。
- `daySnapshot.sessions`: 当日の完了済み通常値引セッション。
- `daySnapshot.review19Check`: 19:00チェック本体。
- `daySnapshot.areaCountRecords`: 当日の通常値引時の残数入力履歴。

現在の新規保存では以下を入れない。

- トップレベルの19時30分用 `reference`
- `review19Check.reference`
- `snapshot.reviewReference`

ただし17時値引セッションの `snapshot.session.weather` は、17時判断時に入力した15〜21時の時間別予報をそのまま保持する。これは19時に天候を改めて判定したデータではない。

`ratings` / `ratingScores` は旧互換のため残るが、現在の画面入力の中心は `areaCounts`。初期値は `just_right`。

### 出力
- 未出力: `nebiki-review19-unexported-開始日_終了日.json`
- 全データ: `nebiki-review19-all-開始日_終了日.json`
- 未出力10件以上で出力推奨。
- 最後に入力したエリアもstate反映待ちで欠落しないよう、保存時に直接マージする。

## 10. 日付変更・前日状態の復元防止

### タブを開いたまま日付をまたいだ場合
- 起動時の日付と現在日付を30秒ごと、focus時、visibility復帰時に比較する。
- 日付が変わったら更新要求画面を出す。
- 動作確認モードではこのブロッカーを使わない。

### 翌日にブラウザが再起動・再読込された場合
起動時に保存状態を検査する。

- `session.date` が今日でない。
- `session.startedAt` のローカル日付が今日でない。
- 前日のwork checkpointが残っている。

該当時は以下を復元しない。

- 前日の値引画面・完了画面
- work checkpoint
- runtime state
- 戻る履歴

これにより、翌日の15時天候入力後に前日の17時完了画面へ進む問題を防ぐ。

### 日次セッションログ
- `session.date` と `startedAt` のローカル日付が一致しない日次スナップショットは保存・読込対象外。
- 前日のセッションを当日ログへ混ぜない。
- `daySnapshot.sessions` には通常値引セッションだけを入れ、`review19` 系画面は除外する。

## 11. 時刻遷移

開始準備時刻:

- 15時: 前の時間帯から15時扱い。
- 17時: 16:40。
- 18時30分: 18:25。
- 19時30分: 19:25。
- 20時30分: 20:25。

完了画面の「〇時の値引に進む」ボタンは廃止。次の天候入力開始時刻になると自動で天候入力画面へ移動する。作業中の自動遷移では、前時刻の未完了エリアを優先する。20時30分も同様に20:25から天候入力画面へ自動移行し、入力後に最終値引へ進む。20時30分だけ独自レイアウトにはせず、19時30分までと同じ全時間帯グリッドを表示し、21時の入力欄だけを順番に有効化する。

17時作業中の18:00〜18:24は、18時30分基準より5%弱めた先取り値引を使う。先取り済みエリアは18時30分で自動スキップ予約する。

18時30分作業中の19:00〜19:24は、19時30分基準より5%弱めた先取り値引を使う。先取り済みエリアは19時30分で自動スキップ予約する。手動で値引時刻を固定した動作確認では、この実時間による先取り表示を適用しない。

## 12. 天候補正の大枠

- 基本値引率は時刻固定。
- 天候は値引率補正として足し引きする。
- 快適度は5段階: 超快適 -10%、快適 -5%、普通 0%、少し不快 +5%、不快 +10%。
- 17時以降の快適方向は通常最大-5%。
- 雨単発 +5%、雨継続 +10%。
- 雪単発 +15%、起点雪かつその後も雪 +20%。雪時は快適度補正を使わない。
- 正確な計算は `src/domain/weekdayBase.ts` とロジックテストを確認する。

### 最終値引の3段階基準（20時30分）

最終値引は次の3パターンから自動選択する。

- 最低基準 A: 3個以上50%、2個40%、1個30%。
- 中間基準 B: 2個以上50%、1個40%。
- 最大基準 C: すべて50%。

判定順:

1. 20時30分の開始画面で、21時の天気・気温・風速を改めて入力する。
2. 21時が雪ならC固定。金土補正でも弱めない。
3. 21時が雨なら最低B。最終値引判定では雨による体感悪化を快適度1段階分として加え、15度以下で雨補正後が「不快」相当ならC候補にする。雨なしの同じ気温よりCへ寄りやすい。
4. 21時が雨雪以外なら、実際の21時気温・風と快適度で判定する。
   - 普通以下（普通・快適・超快適）: A。
   - 暑さを含む少し不快・不快: B止まり。夜への来店ずれ込みを考慮する。
   - 15度以下かつ快適度スコアが+2以上となる強い寒さ: 月に関係なくC候補。
5. 金曜・土曜は夜客増加を考慮し、原則1段階だけA側へ戻す。
   - C→B、B→A、A→A。
   - ただし雪はC固定。
   - 雨はB未満にしないため、C→B、B→B。
6. 各エリアで20時30分残数を入力し、過去の同じエリア・同じ時刻・同じ曜日（不足時は暫定グループ）の中央値と比較する。
   - 中央値付近: 天候・曜日で決まったA/B/Cを変更しない。
   - 中央値より上寄り（やや多い・多い）: C側へ1段階。A→B、B→C、C→C。
   - 中央値より下寄り（やや少ない・少ない）: A側へ1段階。C→B、B→A、A→A。
   - 雪は残数が下寄りでもC固定。雨は残数が下寄りでもB未満にしない。
   - 20時30分履歴が3件未満のエリアは残数を保存するが、残数補正は行わず天候・曜日基準をそのまま使う。

実装は `src/domain/discount.ts` の `getFinalTimeGuide`。21時天候・21時気温・曜日・20時30分基準の快適度・現在エリアの残数評価を渡す。残数履歴判定は `src/domain/areaCountHistory.ts`。20時30分は各エリアで残数入力画面→最終値引指示画面の順に進む。指示表示は `getFinalTimeInstructionSteps` を使い、Aは「3個以上→2個→1個」、Bは「2個以上→1個」、Cは「すべて」の順に表示する。3基準すべてに共通する「3個以上50%」だけを最初に表示して、基準が変わっていないように見せない。

### npm installの注意

- 配布ZIPの `package-lock.json` にOpenAI内部レジストリURLを入れない。必ず `https://registry.npmjs.org/` を使う。
- プロジェクト直下の `.npmrc` でも公開npmレジストリを固定する。
- `npm install` が15分以上くるくるしたままなら中断し、ZIP内の `package-lock.json` に `packages.applied-caas-gateway` が残っていないか確認する。


### 20時30分完了時の日次データ自動ダウンロード

- 20時30分で全12エリアの残数入力が揃った瞬間、ユーザー操作を追加で要求せず、その日の総データJSONを自動ダウンロードする。
- ファイル名は `nebiki-day-YYYY-MM-DD.json`。スマホのダウンロード領域へ1日1ファイルずつ溜める。
- 出力形式は `nebiki-helper-day-export` version 1。`trigger` は `final-counts-complete`。
- `daySnapshot` には、その日の完了済み通常値引セッション、19:00チェック、15時〜20時30分の残数履歴をまとめる。
- 20時30分セッションは、各エリアの残数評価と最終値引A/B/Cの指示内容も保存する。
- 同じ日付では二重自動ダウンロードしない。端末内の `nebiki-helper/final-day-auto-export-dates` で完了日を記録する。
- `?testTime=...` の動作確認モードでは、本番データ混入とテストファイル蓄積を防ぐため自動ダウンロードしない。
- 実装箇所は `src/hooks/useNebikiApp.ts`、出力payloadは `src/domain/dayExport.ts`、二重出力防止は `src/domain/storage.ts`。

## 13. 直近の重要修正（2026-07-05〜07-14）

- 通常値引の時刻別上限を廃止し、0〜50%だけに統一。
- 新人ステップをstep1〜step8へ再編し、判断要素を1段階ずつ解禁する構成へ変更。
- step2から曜日・時刻基準を表示し、step4では「10個以上=多いではない」を強調。step5は定番・夜売れる商品-10%、step6は見た目・不人気+10%、step7は今日の減り方、step8は広告商品の当日動きを解禁。
- 19:00チェックのスキップを「あとで戻る」並び替え方式に変更。
- 通常残数入力と19:00チェックに足し算電卓を追加。
- 電卓の「足し算電卓」見出しを削除。
- 19:00チェックの説明文・不要ラベル・余白を整理。
- 値引指示を「多い商品」「そのうち10個以上」の1カードに統合。
- 19:00チェックから独立した19時30分基準referenceを削除。
- 通常値引で完了後に `skipReason` を消すよう修正。
- 手動5段階判定と判定元を正確に保存。
- 前日セッション混入を日付・startedAt整合性で除外。
- 起動時に前日完了画面・checkpoint・runtime stateを復元しないよう修正。
- ZIP名を `nebiki-helper-YYYYMMDD-HHMM.zip` に変更し、`fixed` を廃止。
- 引き継ぎメモ更新とChatGPTメモリ更新を明確に区別。
- 引き継ぎメモを1000行以内で維持するルールを追加。
- PWA化し、Galaxyへ独立アプリとしてインストールできるようにした。
- 手動5段階判定画面にも `迷ったら…` を追加。
- 通常残数入力・19:00チェックとも、電卓入力途中でスキップしても同じエリアへ戻った時に式を復元するようにした。
- 最終値引を21時天候・実際の寒さ・快適度・金土補正でA/B/Cの3段階から選ぶようにした。20時30分にも21時予報を入力する。
- 20:25から20時30分用の天候入力画面へ自動移行するようにし、完了画面の次値引ボタンを廃止した。
- npmの内部レジストリURL混入を除去し、`.npmrc` で公開npmレジストリを固定した。
- 20時30分の天候入力画面を19時30分までと同じレイアウトへ統一した。最終値引の指示もA/B/Cごとのまとまりで表示し、Bは「2個以上50%」、Cは「すべて50%」から開始するようにした。
- 20時30分もエリアごとに残数を入力・保存し、中央値付近はA/B/C維持、上寄りはC側へ1段階、下寄りはA側へ1段階補正するようにした。履歴3件未満は補正なし。
- 最終値引では雨による体感悪化を快適度1段階分として加え、雨天の寒さでCへ移行しやすくした。
- 20時30分の残数入力画面では、上部の「基本値引率・値引率補正」パネルを表示しない。エリア名は画面ヘッダーで表示する。
- 20時30分の最終値引指示画面でも、上部の「基本値引率・値引率補正」パネルを表示しない。エリア名は画面ヘッダーで表示する。
- 通常値引・20時30分残数入力・19:00チェックでは足し算電卓を最初から開いて表示する。電卓の`閉じる`を`完了`へ変更し、クリアボタンを削除した。
- 残数入力と足し算電卓を別々の二段階画面にせず、一つの入力画面へ統合した。数字をそのまま入力しても足し算しても、`完了`は1回だけでよい。通常値引では履歴判定が使える場合はそのまま値引指示へ進み、履歴不足時は手動5段階エリア判定を表示する。20時30分はそのまま最終値引指示へ進み、19:00チェックは次のエリアまたは記録完了へ進む。
- 20時30分で全エリアの残数入力が揃った瞬間、その日の全セッション・19:00チェック・残数履歴をまとめた `nebiki-day-YYYY-MM-DD.json` をスマホへ自動ダウンロードするようにした。同日二重出力は防止し、動作確認モードでは出力しない。
- 手動5段階エリア判定の `迷ったら…` は、15時「少ない側に寄せる」と17時以降「多い側に寄せる」の2行だけを表示する。値引指示画面側の詳しい判断基準表示は維持する。
- 値引指示画面の注意欄から「少ない判定の商品は、最終値引以外では引かない」を削除した。18時30分で少ないと判断した商品を19時30分でも固定して除外するような誤解を避け、各時刻で再判定した結果として少ない商品を指示対象に出さない挙動だけを維持する。
- トップ画面右上へ歯車の管理者設定ボタンを追加した。初回に4〜8桁の数字PINを作成し、以後はPIN認証後にstep1〜step8を変更できる。選択stepは端末内に保存され、ハッシュなしで起動するPWA独立アプリでも維持される。
- Android端末などのシステム側「戻る」操作はアプリ全体で無効化した。PWA内の画面移動には各画面に用意したアプリ内の「戻る」ボタンだけを使う。通常の戻るボタン・戻るジェスチャーを行っても、現在画面から移動したりPWAが閉じたりしない。強制終了・端末再起動・アプリ固定の正式解除までは対象外。

このZIP作成時の確認:

- `npm run check:logic`: 88 / 88 checks passed。
- `npx tsc -p tsconfig.app.json --pretty false`: 成功。
- `npm run build`: 成功。Vite本番ビルドとPWA生成を確認。

## 14. ZIP返却前の最終確認

- [ ] ユーザーの依頼だけを変更した。
- [ ] `CHATGPT_HANDOFF.md` を実ファイルとして更新した。
- [ ] `wc -l CHATGPT_HANDOFF.md` が1000以下。
- [ ] `npm run check:logic` が成功。
- [ ] `npx tsc -p tsconfig.app.json --pretty false` が成功し、可能な環境では `npm run build` も成功。
- [ ] ZIPに `.git`、`node_modules`、`dist`、`.env`、秘密情報、別ZIPが入っていない。
- [ ] ZIP名が日本時間の `nebiki-helper-YYYYMMDD-HHMM.zip`。
- [ ] 作成後のZIP内に更新済み `CHATGPT_HANDOFF.md` が入っている。

## 15. 既知の注意

- `README.md` は過去仕様の説明が一部残っている可能性がある。最新判断では、本メモと実コードを優先する。
- 旧保存データ・旧出力JSONには、現在削除済みの `reference` や完了後の `skipReason` が残ることがある。新規保存の挙動と区別する。
- 互換用の型・関数が残っていても、現在のUIから使っているとは限らない。削除前に参照箇所を検索する。
