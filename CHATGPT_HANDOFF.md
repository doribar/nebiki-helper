# ChatGPT 引き継ぎメモ

このファイルは、値引きヘルパー本体 ZIP に同梱するための引き継ぎ用メモです。次の ChatGPT セッションでは、ユーザーがこの ZIP をアップロードしたら、まずこのファイルを読んでから作業してください。

## 最重要ルール
- この ZIP の中身を、現時点の値引きヘルパー本体の正本として扱う。
- ZIP 名ではなく、ユーザーが次セッションに渡した ZIP の中身を正本として扱う。
- 同一セッション内で ChatGPT が新しい ZIP を生成した場合は、最後に生成した ZIP を最新の正本として扱う。
- ChatGPT が ZIP を更新して返すたびに、この `CHATGPT_HANDOFF.md` も必ず更新する。
- 会話上限対策として、引き継ぎは会話末尾の別プロンプトではなく、このファイルを読む前提にする。

## 軽量 ZIP の方針
配布 ZIP には次を含めない。

- `.git/`
- `node_modules/`
- `dist/`
- `.env`
- APIキー・秘密情報
- 実写真データ
- 一時ログ

依存関係は `package.json` と `package-lock.json` から復元する。

## Windows 11 での基本起動
PowerShell で以下を実行する。

```powershell
Expand-Archive .\nebiki-helper-no-ai-holiday-fix.zip -DestinationPath C:\nebiki-helper -Force
cd C:\nebiki-helper\nebiki-helper
npm install
npm run dev
```

ロジック確認:

```powershell
npm run check:logic
```

ビルド確認:

```powershell
npm run build
```

## 現在の技術構成
- React
- TypeScript
- Vite
- ローカル保存は主に `localStorage`
- 本体の中心は `src/hooks/useNebikiApp.ts` と `src/app/AppRouter.tsx`
- 値引きロジックは `src/domain/discount.ts` と `src/domain/weekdayBase.ts`
- 日本の祝日・休日判定は `src/domain/japaneseHoliday.ts`
- エリア・巡回・後回しは `src/domain/area.ts` / `src/domain/pending.ts` / `src/domain/navigationHistory.ts`

## 2026-05-03 現在の大きな方針変更
ユーザー判断で、AI写真判定はやめることになった。

今回の修正で、以下を削除した。

- 写真撮影画面
- AI写真判定サーバー連携
- 写真判定サーバー同梱フォルダ `nebiki-photo-judge/`
- `src/domain/photoJudge.ts`
- `src/domain/photoCapture.ts`
- `src/domain/photoCaptureStore.ts`
- エリア判定画面のAI参考判定パネル
- AI参考判定の再送信ボタン
- 写真判定フィードバック保存
- 写真判定サーバーURL設定

開始フローは、通常値引なら `start -> area_judge` に戻した。20時30分は従来どおり `final_time`。

## 祝日まわりの曜日基準ルール
今回、祝日判定を追加した。

### 追加ファイル
- `src/domain/japaneseHoliday.ts`

### 実装したルール
祝日・振替休日・国民の休日を日本の休日として扱う。祝日判定はアプリ内で計算する。

祝日当日の曜日基準は、天気・気温・風などの補正をかける前に、まず以下で決める。

- 祝日の15時: 日曜日扱い
- 祝日の17時以降で、翌日も休日・祝日なら: 金曜・土曜扱い
- 祝日の17時以降で、翌日が平日なら: 火曜・木曜扱い

ここでいう「翌日も休日・祝日」は、土曜・日曜・日本の祝日/振替休日/国民の休日を含む。

その後、通常どおり曜日基準補正をかける。

例:

- `2026-05-05 15時` → 祝日の15時なので日曜日基準
- `2026-05-05 17時` → 翌日 `2026-05-06` も振替休日なので金土基準
- `2026-01-12 17時` → 翌日が平日なので火木基準
- `2026-09-22 17時` → 国民の休日で翌日も秋分の日なので金土基準

### 注意
`src/domain/japaneseHoliday.ts` の春分・秋分は1980〜2099向けの近似式で計算している。通常運用年では実用上問題ない想定だが、将来の法改正や特別な祝日移動は別途反映が必要。

## 値引きヘルパー本体の主な仕様
- 15時 / 17時 / 18時30分 / 19時30分 / 20時30分の値引き運用を扱う。
- 曜日基準、祝日、天気、気温、風、雨・雪による補正を扱う。
- 20時30分の最終値引は `30 / 40 / 50%` の3択固定。
- 20時30分開始時は天候入力を行わない。
- エリア順に巡回する。
- `少ない` と手動 `スキップ` は pending / 後回しとして扱う。
- pending の巡回順は、理由グループよりも現在位置からの近さを優先する。
- 戻る / 取り消しでは、画面状態だけでなく、次回値引記録や前回天気記録の復元も確認する。

## 補正の基本方針
- 「曜日基準で補正しきれない分」の補正上限は時間帯で変わる。
- 強める側は、15時は `+5%` 上限、17時以降は `+10%` 上限。
- 緩める側は、15時は `-10%` 上限、17時以降は `-5%` 上限。
- ただし、30分〜1時間後の雨 `+10%` / 雪 `+20%` があるときは、この「補正しきれない分」は重ねない。
- 最終値引は固定表示。点数計算は現状使っていない。

## 時間別お天気入力
- StartScreen では、ウェザーニュースの1時間ごと表示を見ながら 15〜21時 の天気 / 気温 / 風速を入力する。
- 15時値引のときは、15時と17時の予報を比較する。
- 17時の気温が15時より5度以上低いときは曜日基準 +1段。
- さらに、上の条件を満たしたうえで17時の風速が15時より強いときはさらに +1段。
- この補正は通常の曜日基準補正に合流する。

## pending / 後回しの巡回方針
- `少ない` を選んだエリアは、後回し (`postponed_few`) を維持する。
- 手動スキップ (`skipped_manual`) も pending に残す。
- pending を回る順番は、`manual` / `few` の理由切り替えよりも現在位置からの近さを優先する。
- pending 中に同じエリアをもう一度 `少ない` / `スキップ` した場合は、そのエリアだけ一時的に後ろへ回す。

## 今回の確認結果
- `npm run check:logic` PASS。主要チェックは `45 / 45 checks passed`。
- `npm run build` PASS。

## 次の ChatGPT への作業指示
1. ユーザーがこの ZIP をアップロードしたら、まずこの `CHATGPT_HANDOFF.md` を読む。
2. ZIP 内の `nebiki-helper` を最新の正本として扱う。
3. 修正する場合は、ソースを直接更新する。
4. ZIP を返すときは `.git` / `node_modules` / `dist` / `.env` / 秘密情報 / 作業ログを含めない。
5. ZIP を返す直前に、このファイルを現状に合わせて更新する。
6. 可能なら `npm run check:logic` と `npm run build` を実行し、結果をユーザーに伝える。
